# Update Log

## 2026-08-26
* **Crash / a THIRD family: `DEVICE_HUNG` at a graphics-setting change**: DLSS and, separately, ambient occlusion each took the device down from the settings menu. Signature is distinct from both known families: `0x887A0005` with reason `0x887A0001` (`DEVICE_HUNG`), but **no** `gpucrash` log, **no** fence timeouts, **no** breadcrumbs (`GetAutoBreadcrumbsOutput` returned `0x887A0004`, driver internal error), `PageFaultVA=0`, and — checked in the Windows event log — **no `nvlddmkm` event at the crash time**, so the kernel driver logged no reset. The `nvlddmkm 153` entries that day belong to an earlier crash 2h43m before and were deliberately not used. **No dump exists and none can**: REDEngine wrote none and ProcDump would not either, because a device-removed shutdown raises no exception. The log is the entire evidence base.
* **Correction / my own logging hid the answer, in three places at once**: The crash sequence was `SetFullscreenState` -> `Overlay load guard engaged` -> device dead, six lines. Which of the **five** call sites armed the guard could not be determined, because (1) `OverlayArmLoadGuard` took a `reason` and did `(void)reason`; (2) `pso_note_creation` throttled on a session-wide counter, `arms <= 12 || arms % 100 == 0`, so it logged twelve boring startup arms and then went silent for eighty-seven — including the arm at the crash; (3) `ResizeBuffers` logged only when the forced size differed from the requested one, and a settings change asks for the size already forced, so the single call that releases every overlay render target and arms the guard left **no trace whatever**. All three suppressions were deliberate and each was individually defensible; together they blinded precisely the window the crash happens in. Fixed as logging-only: the reason is stored and printed on the **engage edge** (one line per window, not per arm), the PSO throttle now keys on **quiet** (`>= 2000 ms` since the previous arm) rather than a counter — startup stays quiet for free while the interesting post-quiet burst always prints — and both `ResizeBuffers` and `ResizeBuffers1` log unconditionally.
* **Result / the instrumented session did NOT crash, and it weakens the leading hypothesis**: Several settings changed plus a save load, clean. **107 PSO bursts and 76 guard engagements**, so if arming a full drain during a graphics-setting change were fatal by itself it had ~15 post-warm-up chances and survived every one. It also survived a **mid-session** `SetFullscreenState`+`ResizeBuffers` pair, the same event that preceded the crash. So "the PSO-burst arm kills the device" does not hold as stated, and the trigger is **intermittent**, not deterministic — which is the opposite of what the two clean repros suggested. Recorded rather than acted on: nothing further ships until a crash arrives carrying the new `armed by:` line.
* **Confirmed / `SetFullscreenState` always pairs with `ResizeBuffers`**: 175->176, 177->178, 1371->1372 in the clean session. So the crash session's line 1802 was near-certainly followed by a resize that released every overlay render target and armed the guard; the old build simply could not show it. `SetFullscreenState(FALSE)` itself passes straight through — only `TRUE` is refused — so the hook is a marker here, not a cause.
* **Result / accidental photo mode is fixed, and input_loader REPLACES rather than appends**: Confirmed in play — clicking both sticks no longer opens photo mode — and then confirmed in data, which was the point of shipping `CyberpunkVRPort_NoPadPhotoMode.xml` as a measurement. The merged `r6\cache\inputUserMappings.xml` holds **exactly one** `TogglePhotoModeButton` mapping carrying **only `IK_N`**, and **no mapping anywhere still binds `IK_PAD_LR_THUMB`**. So a mod mapping that reuses an existing name **replaces** the game's entry; input_loader's log showing only merge operations and never a removal does not mean it cannot remove. That answers a general question about the whole input layer, not just this binding. **Slide is intact** — nothing about sprint or crouch was touched, which was the whole reason for fixing it at the binding rather than by suppressing the two clicks. The `IK_PAD_LR_THUMB` **buttonGroup declaration** still exists in the cache and correctly so: it is the chord's definition, not a binding, and nothing maps to it. Escape hatch re-verified in the same read: `ExitPhotoModeButton` still carries `IK_Escape`, `IK_N` and `IK_Pad_B_CIRCLE`.
* **Correction / the chord was never "no time window"**: An earlier entry and the XML's own comment said `IK_PAD_LR_THUMB` had no time window. It does — `timeWindow="0.1f"`. The conclusion is unchanged and if anything stronger: the port emits `LEFT_THUMB` and `RIGHT_THUMB` in the **same frame**, so a 0.1 s window is no protection whatever. Also corrected: `LockCameraButton` is `IK_T` **and** `IK_Pad_Y_TRIANGLE`, not Triangle alone — the Triangle/D-pad-modifier collision inside photo mode still stands, and `PhotoMode_ShowHideUI` sits on `IK_Pad_RightThumb`, though both are only reachable once already inside.
* **Creation / [Render FOV, cover versus span](fixes/fov-cover-versus-span.md)**: The FOV work existed only as log entries, which [okf-maintenance](operations/okf-maintenance.md) explicitly warns against — the root log is a chronology, not the documentation. Promoted to a concept: the `2 * max(|angleLeft|, |angleRight|)` cover model and why it renders a canted frustum together with its own mirror image, both measured headsets, the per-headset edge cost (PSVR2 outer horizontal only; Quest 3 also ~5 deg short at the bottom), the full precedence chain `xr_force_fov` -> `xr_fov_mode` -> runtime fallback, and the recipe for reproducing mode 1 on stock upstream from its own ungated `deCantedHFov` line.
* **Creation / [Second-eye load crash](fixes/second-eye-load-crash.md)**: Same gap, and worse for being an **open** problem spread across a week of log entries — the disassembly proof, the real `cdb` unwind, the four eliminated hypotheses and the two failed fixes were each recoverable only by reading the chronology in order. Now one concept, stating plainly what is established (a frame-graph node dispatched for the second eye consumes a `-1` descriptor index in the post-rebind window), what is ruled out and by what test, and the single piece of evidence still missing: a faulting thread id paired with a `[rebind-trace]` from the same session. Records the `-e` not `-t` ProcDump distinction that has already cost one capture, and separates this family explicitly from the GPU-hang family it keeps being confused with.
* **Result / Quest 3 span mode works in play**; `xr_fov_mode` ungated and defaulting on is confirmed good on that headset. No black-border complaint reported at 94, so the ~5 deg bottom shortfall computed for its `-5.5` pitch cant appears to sit behind the lens mask in practice. `xr_force_fov=100` remains the middle dial if it ever shows.
* **Correction / `AUDIO_GAIN=0` caused the one-handed engine feel**: Driving engine texture being felt in the **right hand only** is a direct consequence of setting the audio layer to 0, not a fault. The engine rumble in **both** grips *is* that layer — `vr_motion_haptics.h` describes it as "stereo-derived, it buzzes both grips" — so with it off the only vehicle feedback left is the DSX **trigger** effect on R2, which is one hand by construction. Verified the layers are independent: `HapticsEngine` (grip PCM) is the transport and starts on its own; `gameAudioGain` scales only `AudioHapticsCapture`.
* **Both gains now set from measurement rather than a guess**: the dial is bracketed at both ends in play — **1.35** masks weapon character (and made a weapon-category override look like it did nothing), **0** restores weapon character but costs the engine and the left hand. Set to **0.5**. Melee raised **1.0 -> 2.0**: the bridge computes `amplitude = raw * gain` clamped to 1, and the plugin sends a threshold swing at raw `0.45`, so 1.0 delivered 0.45 of full scale — 2.0 puts a threshold swing near 0.9. Both values and their evidence are now in `run_bridge.cmd` itself.
* **Note / Quest 3 driving is a different path and is fine**: it uses the plugin's own in-process vehicle audio worker (`xr_openxr_vehicle_haptic_gain`, 1.00), not the bridge, which is why it was unaffected by any of this. The missing adaptive-trigger feel there is hardware: Touch controllers have no adaptive triggers, so nothing is wrong to fix.
* **Change / `xr_fov_mode` ungated and now defaults ON**: The PSVR2-only gate existed because PSVR2 was the only headset measured. A Quest 3 session on this machine supplied the second measurement, so the gate is gone and the default is `1` (sharper). Crucially the mode is **not a per-headset constant** — `GetCorrectedGameHorizontalFovDeg` derives the value from the **connected headset's own reported frusta**, so it self-adapts and consults no name. That also sidesteps the trap `kHeadsetFovDefaults` documents, where VDXR reports a Pico 4 as `"Oculus Quest2"` (this Quest 3 reports honestly as `"Meta Quest 3"` over VirtualDesktopXR, but relying on that would be luck). The F10 checkbox now shows on every headset. `xr_fov_mode=0` restores upstream cover sizing exactly.
* **Measured, both headsets, from their own `[FOV]` lines**: PSVR2 frusta `-61.500/+43.446`, cover **123.000** -> span **104.946**, ~17% more linear resolution. Quest 3 frusta `-54.000/+40.000` at 2864x3184, cover **108.000** -> span **94.000**, ~15%. In both cases roughly a third of the rendered **solid angle** fell outside the lens.
* **The cost differs by headset, and that is worth knowing before enabling it**: the submitted frustum stays symmetric, so whichever side the cant favours goes short. PSVR2 is vertically symmetric (`U=D=53.040`), so only its **outer horizontal edge** is exposed. The Quest 3 is canted **down** as well (`U=44.000 D=-55.000`, `correctionPitch=-5.500`), so at span its **bottom edge sits ~5 deg short** while the top gains 6 — computed: half-V 50.01 against a 55 deg bottom lens. `xr_force_fov=100` is the middle dial there (~8% of the 15%, bottom short by only 2 deg).
* **For anyone on stock upstream**: `xr_force_fov` is ungated and has always existed, and upstream 0.1.5 prints `deCantedHFov` in an **ungated** `Log()` — so a plain upstream user can read their own number out of `bin\x64\cyberpunkvrport.log` and set `xr_force_fov` to it with no new build. Caveat: it is not headset-aware, so it must be cleared to `0.000` when swapping headsets or it will crop the other one.
* **Finding / photo mode is bound to L3+R3, and the port synthesises both**: Screen going 2D with camera controls in the lower right is **photo mode**, not a stereo fault. Read out of the game's own merged `r6\cache\inputUserMappings.xml` rather than assumed: `TogglePhotoModeButton` is `IK_N` **and `IK_PAD_LR_THUMB`** — both stick clicks together. The port synthesises `XINPUT_GAMEPAD_LEFT_THUMB` for **sprint** (`XInput.cpp:1143`) and `XINPUT_GAMEPAD_RIGHT_THUMB` for **crouch** (`:1144`) and for the scanner **tag** (`:1159`), and **nothing suppresses the two co-occurring**. So sprinting while crouching, or tagging while sprinting, hands the game `L3+R3` and opens photo mode — no deliberate input required. The D-pad chord (hold L3, right stick) plus a right-stick click reaches it the same way. **Exit** is `IK_Escape`, `IK_N`, or `IK_Pad_B_CIRCLE` — Circle on Sense, which already carries XInput B. Also worth knowing inside photo mode: `LockCameraButton` is `IK_Pad_Y_TRIANGLE`, and Triangle is the PSVR2 D-pad modifier, so gestures collide there too.
* **Not yet fixed, deliberately**: the obvious guard — never emit `LEFT_THUMB` and `RIGHT_THUMB` in the same frame — would also suppress **slide**, which in this game is sprint plus crouch and is a legitimate move. Suppressing a real action to block an accidental one is the wrong trade to make unasked, so this is recorded rather than patched pending a decision on which of the two loses.
* **Correction / the presets are NOT flattened**: An earlier entry read `weaponsSettings` all at `value:1` as "stock default everywhere". Wrong — the bridge console shows `L2 <- Choppy` and `R2 <- Bow` for a submachine gun, so `value:1` selects **that weapon's own default from the mod**, and per-weapon differentiation is live. The pipeline works end to end; the config was never lost.
* **Finding / why an automatic specifically feels dead**: `firingBreaks[side]` is set when Enhanced DualSense Support flips a loaded Bow/Weapon profile to `Resistance` or `Machine` — the shot edge. On that edge the bridge sets the trigger to `SCE_PAD_TRIGGER_EFFECT_MODE_OFF` and fires a recoil pulse, per its own note: "the best PSVR2 gun implementations ramp and plateau while pulling, then drop resistance at the actual shot." That is right for a semi-auto. On an SMG at ~10 rounds/s the motor is re-loaded and switched off continuously — the observed console alternates `R2 <- Bow` with `R2 <- Machine -> firing release` — so no sustained resistance ever establishes and the trigger reads as flat. The effect is **weapon-class specific**, which matches the report that some weapons kept their nuance and the SMG did not.
* **Note / L2 and R2 are two CONTROLLERS on Sense, not two triggers on a pad**: `sideValue` 1/2 routes to Left/Right. Enhanced DualSense Support authors LT as the aim trigger and RT as fire on a single pad; on PSVR2 those land on opposite hands, so the SMG's `Choppy` is being applied to the empty (aim) hand while the firing hand gets `Bow`. Restoring the old feel therefore means overriding the **weapon category** so the firing trigger carries the choppy profile — which is exactly the override the 2026-08-11 reversal withdrew. That reversal was measured under a UDP conflict that no longer exists, so it is the thing to re-test, not the config.
* **Investigation / PSVR2 haptics: the weapon config is NOT lost, the melee path DID change**: Two separate questions, two different answers. **(1) Trigger presets.** `config/settings.json` has every weapon at `value:1` — the `default` entry is also `value:1`, i.e. "Default Trigger Effect" across the board. That is not a lost config, it is exactly what the **2026-08-11 reversal** prescribed: the differentiated profile (Handgun Very Soft / Shotgun Hard / SMG Choppy) had been measured while Enhanced DualSense Support's UDP client was autostarting alongside the bridge, and with only the bridge running "the presets degrade haptics and the stock defaults are correct". The SMG's choppy feel was deliberately given up, not lost. The three locked files are intact — `ManageSettings.lua` and `SetupNativeSettings.lua` still dated 08-11, `UDPautostart:false` — so that guard held across every deploy. If stock now feels flat in play, the thing to re-examine is that reversal, not the file.
* **(2) Melee/swing haptics: the mechanism changed underneath them.** On `psvr2-tweaks` the katana swing fired **straight at the OpenXR runtime** via `SetVRHapticPulse` (its own comment says so), which worked for swings but is precisely what [psvr2-adaptive-triggers](operations/psvr2-adaptive-triggers.md) records as costing "most of the game's gun haptics". This branch made PSVR2 **reject** the OpenXR path — restoring gun haptics — and instead publish slots `[157..160]` for the Toolkit bridge. The `kHaptic*` slots do not exist on `psvr2-tweaks` at all, so this is a genuinely new contract. **Both sides check out**: the plugin advertises magic `18512` at `[168]`, version `1` at `[169]`, and increments the heartbeat at `[170]` **unconditionally** in `FlushHandsToShared()` (every frame, every headset — it identifies shared memory and never calls OpenXR haptics); the bridge's `vr_motion_haptics.h` documents the same layout, and the running exe (built 08-23 15:50:17) is one minute older than `cyberpunk-safe` HEAD `35acb6a` (08-23 15:51:21), so it has the support.
* **Next check is free and decisive**: the bridge's watcher prints its own state — `"VR motion haptics protocol v1 active (driving-safe slot layout)."`, `"VR motion haptics paused: CyberpunkVR heartbeat is stale..."`, or an incompatible-marker line. Its console says which of the three applies. Note the running bridge has been up since **2026-08-24 18:34** across dozens of game launches and plugin rebuilds; restarting it is the documented practice ("Restart the bridge rather than using the mod's Restart UDP Client command") and costs nothing.
* **Upstream merge / 0.1.5 fixes the device camera, and better than the plan**: Merged upstream `0.1.5`. It solves what this fork spent a day failing to detect from geometry, and independently confirms the conclusion: the divergence is in **content, not transform**. Two parts. `vrport_device_cam.reds` replaces `Device.ToggleCameraZoom` verbatim **minus** the single `m_cameraZoomComponent.Activate(blendTime)` call, so a computer never takes the camera and the head keeps steering **both** eyes — better than the planned mono fallback, which would have kept the takeover and just hidden half of it. Cost is the zoom-to-screen, reversible via `VRPortDeviceCamZoomWanted()`. `RemoteCamera.cpp` and the `VRRemoteCamera` native handle surveillance cameras: the plugin cannot tell **which** camera was taken over — upstream measured **20559 identity changes cycling between four objects in one session** — so the script side publishes `TakeOverControlSystem.GetControlledObject` four times a second and the camera writer follows only a `cameraComponent` within 1.5 m of it. Also in 0.1.5: PhysicalRay reworked again (calls the engine evaluator **first**, so a foreign ray costs zero syscalls — supersedes 0.1.4's reordering), the wrist readout surviving a weapon draw, and a vehicle view that no longer holds a frame while the car turns.
* **Merge / six conflicts, all additive**: Resolved by taking upstream's structure and keeping this fork's PSVR2 work inside it. **D-pad**: 0.1.5 dropped the Triangle/thumbrest modifier for an L3-only chord; kept as `(leftDpadTouchModifier || leftStickClicked)` because SteamVR's Sense remapper omits the buttons that chord needs ([psvr2-triangle-dpad](fixes/psvr2-triangle-dpad.md)), while taking upstream's tunable to-the-stop threshold. **Scanner**: `!g_isInVehicle` preserved inside upstream's restructured gesture. **Vehicle**: upstream's X-hold exit and A-mirrors-X dialogue confirm are additive to Circle already carrying B on Sense, so both paths kept. **FirstLaunch**: upstream's cascade-drift guard ordered **before** this fork's reinstall preservation, since a drifted cascade is a correctness fault the two views cannot be left with and outranks preserving tuned graphics. Builds clean; every item of this fork's work verified present afterwards.
* **Bug found in `deploy_stereo.ps1` / a green deploy installed none of the fix**: The first 0.1.5 deploy reported success while installing **none** of the new content. The CET loop is a hardcoded refresh list that deliberately skips folders the game lacks, and **redscript was never copied at all** — so `vrport_device_cam.reds`, the actual camera fix, never reached `r6\scripts`. Extended the script to copy all of `mods/redscript` wholesale, to **create** missing CET folders (logging each, since "the repo has a mod the game does not" is exactly what a dev deploy should close), and to install `mods/config/input`. Re-deployed: 17 redscript files, 4 new CET mods, 1 input config. Verified in place afterwards rather than trusting the exit code — a merged, built and deployed fix that the game does not have is the failure mode where success means nothing.
* **Deployment**: Installed the 0.1.5 merge (`f07368cb080c287d...`) plus the new archive (1,634,304 B); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260826-072149-pre-0.1.5` (DLL, ini, archive). **Untested in play.**

## 2026-08-25
* **Result / four negatives: the port cannot currently see a device takeover at all**: Session run deliberately as camera-control hack first, laptop last. **`sceneTier` never moved** — only `0 -> 0` and `0 -> 1` at startup, then nothing, so a device takeover does **not** change the gameplay tier and that is not the trigger. **`PatchCamera[probe]` fired zero times**, meaning `resid` stayed within 1 m of one IPD throughout: the stereo pair never geometrically diverged, which **confirms the earlier 928 m reading was purely the unsynchronised-sampling bug** and retires the geometric approach entirely. `mountedVehicle` stayed `mounted=0` (takeover does not mount). The per-view render mask is byte-identical first line to last. So none of the signals the port already hooks respond to a takeover, and no threshold on any of them could have worked.
* **Consequence / the divergence is in CONTENT, not transform**: The port patches MAIN and VRCAM to the player's camera one IPD apart and that stays correct during a takeover — `resid` proves it. The game feeds MAIN the device's image through a **different view**, which is why the eyes show different pictures while their transforms agree. Any fix therefore has to know *that a takeover is happening*, not measure where the cameras are.
* **Proposed path / use the existing redscript-to-plugin bridge**: The port already exposes a large `SetVR*` native surface ([Registration.cpp](../src/Natives/Registration.cpp)) that CET and redscript call into. A redscript hook on the device-takeover event calling a new `SetVRRemoteView(1/0)` would give an explicit, reliable signal where four inferred ones failed, and it feeds the mono gate already built and proven in `CyberpunkVR_GetVrcamEyeTextureFresh`. Not implemented: the correct game class/event to hook has not been verified, and after two failed inference-based attempts nothing further ships without that verification.
* **Diagnostics / camera takeover, observation only**: Two probes added after the view-split detector failed, both changing no behaviour. (1) **`[VR][tier]` scene-tier edges.** `PlayerPuppet.sceneTier` has been sampled every frame in [LocateCamera.cpp](../src/Hooks/LocateCamera.cpp) since it was added and **never written to a log**, which is why no capture could answer whether a camera takeover changes player state. It now logs on change (1 full, 2 staged, 3 limited, 4 FPP-cinematic, 5 cinematic). A device takeover locks movement, so it should read above tier 1 — and if the value is distinctive that becomes the trigger: an explicit state the game itself publishes, rather than a distance inferred from two camera positions captured at different instants. (2) **`PatchCamera[probe]`**, which fixes both mistakes of the failed detector — it samples only on `camKind == 1` (the gate the existing diagnostic already uses, making the pair comparable) and reports **`resid`**, the metric the file documents as the alignment number, rather than raw `sep`. It confirms over 30 consecutive samples either way, so no transient can latch it. `CyberpunkVR_ViewProbeMetres` (1.0) sets the reporting threshold; `CyberpunkVR_ViewSplitMetres` stays **0**, and only setting it non-zero promotes the same confirmed signal into the mono gate — no rewrite needed if the evidence supports it.
* **Deployment**: Installed the camera-probe DLL (`15eebeda6be5ac10...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-193243-pre-camera-probe`.
* **Correction / the view-split mono fix was WRONG and is disabled**: It shipped the game in mono and caused left-eye flashing. The detector read `s_mainPosFP` against `s_vrcamPosFP` on **every** camera write, but those two are only mutually consistent when the write being handled is MAIN — which is exactly why the diagnostic beside them gates on `camKind == 1`. Without that gate it differenced a fresh position against a stale one and alternated on consecutive frames: `SPLIT 915.1 m` / `rejoined 0.1 m` / `SPLIT 915.1 m` / `rejoined 0.1 m`, disowning the second eye roughly half the time. That is both the mono and the flashing the user reported — the eye was handed a texture on one frame and refused on the next, at frame rate. `CyberpunkVR_ViewSplitMetres` now defaults to **0**.
* **Correction / the evidence behind it is also in doubt**: The 928 m "camera hack" separation came from that same unsynchronised pair, and `mainPos` sat at `(-738.9, 2160, 53.9)` in earlier sessions with **no** camera hack — an anomaly noted at the time and not chased. So there is currently **no sound measurement** that MAIN and VRCAM diverge during a camera takeover. The mechanism may still be real (MAIN follows the takeover, VRCAM stays on the player, which fits the reported symptom) but it is unmeasured. `PatchCamera.cpp` warns explicitly that raw `sep` is not an alignment metric because it carries head displacement and inter-write motion; the argument that a 5 m threshold cleared that warning was **wrong** — a large threshold guards against noise, not against comparing two values captured at different instants. Any retry must sample both positions at one instant (gate on `camKind`, or snapshot the pair together) and must be confirmed against a log captured with a camera actually hacked.
* **Deployment**: Reverted to `752848906033aaa1...` with the detector disabled; backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-192623-pre-viewsplit-revert`. The FOV clarity fix, PSO-burst guard, depth-submit persistence and tid breadcrumb are all untouched.
* **Fix / mono while the view is split (camera hack, laptops, shards)**: The user's suggestion — detect the state and submit mono — is implementable because the split is enormous and unambiguous in data already being logged. Measured 2026-08-25: in normal stereo MAIN and VRCAM sit **one IPD apart** (`mainPos=(-768.382,63.775,9.483)` vs `vrcamPos=(-768.356,63.722,9.482)`, 0.06 m; 0.059 at the menu, the runtime IPD to three decimals). While a security camera was quickhacked the same pair read `(-738.917,2159.969,53.945)` against `(-1657.001,2029.694,19.964)` — **928 metres**, with `mainPos` frozen at the camera while `vrcamPos` tracked the player. Four orders of magnitude. Now checked on every camera write (not the 900-hit diagnostic throttle, which would lag the takeover) and, past `CyberpunkVR_ViewSplitMetres` (5 m, 0 disables), the second eye is disowned in `CyberpunkVR_GetVrcamEyeTextureFresh` — which routes into the **existing** staleness path and submits mono, per that function's own rule that "one eye live and one eye stuck is far worse than plain mono". No attempt is made to move VRCAM to follow: it is a render-to-texture component attached to the player and has no notion of the game switching cameras. Covers laptops and shards for free, since they park MAIN the same way. Both edges log, and `CyberpunkVR_DebugViewSplitFrames` counts.
* **Caveat respected**: `PatchCamera.cpp`'s own comment warns that raw `sep` is **not** an alignment metric — it carries head displacement and inter-write player motion, and was seen swinging to 45 cm while standing still. That warning is about centimetres and is correct; the 5 m threshold sits ~180x above the observed split floor and far above anything head movement or a single frame of sprinting can produce. The question asked here is "is MAIN even in the same place", not "are the eyes aligned" — `resid` remains the right number for the latter.
* **Deployment**: Installed the view-split DLL (`1e1334555badb94b...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-191941-pre-view-split`. **Untested in play.**
* **Confirmed / the crash happens inside second-eye node dispatch after a re-bind**: The `[rebind-trace]` breadcrumb caught the 19:08 crash. The log **ends mid-trace**, 199 dispatches after the last re-bind (line 1964 of 2163) with ~200 of the 400 budget unspent, and the guard was still engaged (12 engaged / 11 released). Family markers agree it is the descriptor crash and not the hang: **0** fence timeouts, **0** null-arg skips, no `gpucrash` log, 0 `nvlddmkm`, no dump (ProcDump not running). Together with the cdb unwind this closes the mechanism: the -1 is consumed inside a frame-graph node dispatched for the **second eye** in the window immediately after a VRCAM component re-bind. The traced nodes are the ordinary scene set — `SceneDrv_ALL_SCENE_PASSES`, `ViewSetup_perview_RT_state`, `RenderElements`, `UnbindGlobalConstants` — nothing exotic.
* **Limitation found in my own instrumentation**: the trace budget is shared across threads, so several render threads interleave and **the last line is not necessarily the faulting node**. Added `tid=` to each trace line so the faulting thread can be followed alone — either matched against a dump's faulting thread id, or simply identified as the thread whose lines stop. Without it the 19:08 capture cannot name the node, only the window.
* **Camera quickhack / eyes show different scenes — mechanism identified**: Taking a security camera renders **MAIN from the hacked camera while the VRCAM component stays on the player**, so the two eyes genuinely display different scenes; `g_main_view_obj` is re-latched from whatever view the Present/StartRender nodes carry ([NodeDispatch.cpp](../src/Stereo/NodeDispatch.cpp)), so MAIN follows the takeover and the second eye does not. This is a design limitation rather than a regression: the second eye is a render-to-texture component parked on the player entity and has no notion of the game switching its active camera to a remote entity. The contained fix is to detect remote-camera view and **submit mono for its duration** rather than try to move the VRCAM component — mono during a camera hack is acceptable, mismatched eyes are not. Not implemented: the detection point has not been established yet and no more blind changes.
* **Deployment**: Installed the tid-tagged breadcrumb DLL (`aebc1aca84ddd903...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-191126-pre-trace-tid`.
* **Result / the load that always crashed did NOT crash**: First session on the PSO-burst trigger. Two save loads including the one that has reproduced the `+0x1F51F5` crash all day, two vrcam re-binds, and **0** `skipped ExecuteIndirect`, **0** fence timeouts, no crash. The trigger fires exactly as intended and the bursts are as tight as predicted — `16 pipeline states in 16 ms`, `in 0 ms`, `in 141 ms`, `in 47 ms`, `in 63 ms` — arming the guard 12+ times during startup and loading. **The cost is acceptable and measured**: guard engaged 28 / released 28, so it is not stuck open (the failure the concept file warns to check for), and `POSEDIAG` shows 81-84 fps at the end against a 90 Hz display. One session is not proof, but this is the first time that load has survived.
* **Not a crash / the "went 2D and froze" was SteamVR ending the session**: The VR log ends with an orderly `Session state -> 6` (STOPPING) → `Session ended.` → `-> 1` (IDLE) → `-> 8` (EXITING) → `VR submit thread exiting`, and the game **kept running** afterwards — `[wide]`, `[rtt] view-create`, `PatchCamera` and `Unifix` all continue past it. So the process never died; the OpenXR session was terminated and rendering fell back to flat on the monitor, which is what "everything went 2D" is. SteamVR itself was healthy throughout: `vrserver` still running, no crash dump, no `Application Error`, no Bluetooth/USB/GPU events. `xrclient_Cyberpunk2077.txt` shows the app **flapping between FOCUSED and VISIBLE eleven times in 45 seconds** (18:53:01 → 18:53:43) before `SYNCHRONIZED → STOPPING → IDLE → EXITING` at 18:53:44 — i.e. something in SteamVR repeatedly took input focus and the app was then asked to exit. **Inference, not established**: the user recalls pressing a camera-shift/D-pad combo, and a binding reaching SteamVR's system/dashboard action would produce exactly this focus flapping. Worth checking the PSVR2 binding for an overlap with the reserved system action before treating it as a port fault; nothing in the port can end its own session this way.
* **Crash / settings change, and it is the OTHER family**: Changing ambient occlusion High -> Medium hung the GPU at 18:31:34. This is the `0x887a0006` DEVICE_HUNG family, **not** the `+0x1F51F5` descriptor crash: `gpucrash-2026-08-25_18.31.34.367.log` written, 3 `nvlddmkm` events, 2 overlay fence timeouts, `stale by 641 ms -> submitting MONO`, and breadcrumbs showing `Lighting` *In progress* with every later list *Not started* — `Lighting` being one of the three passes [overlay-load-transition-guard](fixes/overlay-load-transition-guard.md) already names for this shape. The `[rebind-trace]` breadcrumb fired 199 times but at the session's single re-bind (lines 487-687), ~3000 lines before the crash, so it says nothing about this one; it targets the load crash, which did not occur here.
* **Finding / two settings-menu hangs, both in the same uncovered window**: The guard released at line 713 and the crash was at 3657 — **not engaged**, ~3000 lines outside any window, `menu=1`. That matches the 10:04 settings-menu hang exactly. The last `[pso]` line was at 478, from the initial load, so the sight-PSO arm never fires for a settings change either: it only sees the handful of pipelines the port substitutes, not the hundreds the engine rebuilds for a new quality level. So a graphics-setting change is a large churn window with **no trigger arming the guard at all**.
* **Fix / PSO-burst trigger restored (was parked)**: Re-added the drafted trigger, now motivated by two measured crashes rather than reasoning. Counts **all** pipeline-state creation across both D3D12 entry points and arms `OverlayArmLoadGuard("pso burst")` when `CyberpunkVR_PsoBurstArm` (16) creations land inside `CyberpunkVR_PsoBurstWindowMs` (500). PSO creation is near zero in warm gameplay and bursts hard while render targets and shader permutations are rebuilt, so it tracks the churn itself rather than the screen it happens behind — `menu=1` remains rejected as a trigger (59 of 103 samples, would collapse Mode 3 into Mode 0). Every arm logs the rate that caused it so the threshold can be judged on data, and both numbers are live exports. **Failure mode is benign and visible**: too low a threshold means more full drains, i.e. lower frame rate in a configuration already measured stable at 43.8 fps, showing up as guard engage/release churn in the log. Expected to cover settings changes and menu loads; it will **not** touch the `+0x1F51F5` descriptor crash, which is a separate family that gating has already twice failed to fix.
* **Deployment**: Installed the PSO-burst DLL (`4f3f5991d0412808...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-183435-pre-pso-burst`.
* **Breakthrough / the cdb unwind puts our detour in the live call chain**: A real `kvn` unwind (not the scan `read_dump.py` does) of the 08:04:40 dump gives the actual frame chain beneath the fault: `00` the fault at `+0x1F51F5` with `RDX=-1`, `01` `+0x7743C9` reading the -1 out of a descriptor object, `03` game node work, **`04 CyberpunkVR_Stereo!cvr::detail::Detour_NodeDispatch+0x958`**, more game work, **`07` the same detour again** (SceneDrv re-entering the dispatcher per pass, exactly as that file's own comments describe), down to `BaseThreadInitThunk`. So the -1 is consumed **inside a frame-graph node executing under our node-dispatch hook** — not in an unrelated worker, and not in the diagnostics. `!analyze -v` blames `Cyberpunk2077.exe!Unknown`, which is right and unhelpful: the fault is in game code. **Caveat on the symbol names**: the exe ships no PDB, so every `Cyberpunk2077!AK::...` name in that unwind is the nearest export plus a six-digit offset and is meaningless — only the frame *structure* is real.
* **Instrumentation / name the node**: The one fact still missing is which node. Added a `[rebind-trace]` breadcrumb in `Detour_NodeDispatch` that logs node RVA, engine name, depth and scene id for second-eye dispatches after a component re-bind — the only window this crash has ever occurred in — so the log's last line names the node that faulted. Self-limiting by **count** (`CyberpunkVR_RebindTraceCount`, 400, armed at each re-bind) rather than by clock, so steady state costs one relaxed atomic load per dispatch and no session can be flooded. Deliberately a **pure diagnostic**: nothing skipped, reordered or suppressed. Two behaviour-changing gates today produced one worse crash and one cleanly-suppressed symptom with no fix, so this one only observes.
* **Deployment**: Installed the breadcrumb DLL (`4046e3e9a32c1330...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-081558-pre-rebind-trace`.
* **Dump / fifth identical fault, depth submit conclusively dead**: ProcDump `-t` captured the 08:04:40 exit (17.7 GB). Fault is byte-identical to all four before it — `Cyberpunk2077.exe+0x1F51F5`, `RDX=0xFFFFFFFF`, `R15=0xAFFFFFFF50`, read at `...0AB0`, caller `+0x7743C9` — in a session where `[DEPTH] depth submit disabled` was logged and `depth=1` submits were **0**. Depth submit is therefore ruled out with a controlled test, not by argument.
* **Ruled out / the two plugin frames in the scan**: This dump is the first whose stack scan shows `CyberpunkVR_Stereo` at all — `wide_report` (Grading.cpp:1319) and `temporal_report` (Grading.cpp:1294), at adjacent slots +448/+458. They are **not** implicated: both are pure reporting functions that take a mutex, read accumulated census arrays, format a string and log. Neither touches a GPU resource, descriptor or command list, so neither can produce a -1 render-table index. Stale slots on a thread these periodic diagnostics also run on. Recorded so the next reader does not re-chase them.
* **Finding / the narrow gate's 400 ms window is marginal**: The gated draws fire at `sinceRebind=344` and `359` ms — inside a 400 ms window by ~40 ms. In this session the fatal burst fell **outside** it: 42 `skipped ExecuteIndirect` (the old NULL guard catching them) where the previous session had 0 and 9 gated. So the window sometimes covers the burst and sometimes does not — and the crash occurred in both cases, which is further evidence the burst is a co-symptom rather than the carrier. Widening the window would only make the suppression more consistent, not fix anything.
* **Blocked / need a real unwind**: `read_dump.py` performs a stack SCAN, not an unwind, so it cannot establish the call chain that supplies the -1. `cdb.exe` exists at `C:\Program Files\WindowsApps\Microsoft.WinDbg_1.2606.22001.0_x64__8wekyb3d8bbwe\amd64\cdb.exe` but WindowsApps is ACL-locked to this session; it must be run by the user. That unwind is the next real step and no further code changes should be attempted before it.
* **Result / depth submit is RULED OUT, and the narrow gate suppresses the symptom but not the crash**: First properly-controlled test — both fixes verified live this session: `[DEPTH] depth submit disabled (xr_depth_submit=0)` at line 514 with **0** `depth=1` submits against 116 `depth=0` (was 122/75), and the profile survived its 07:21:57 rewrite, so the persistence fix holds. `xr_fov_mode=1` also confirmed live (`targetH=104.946`, not 123.000) and the user reports **clearly improved clarity** — that change is good. The load crash nevertheless recurred at 07:24:28 after 414 s of play, on a menu-initiated load: `menu=1`, then `re-bound vrcam component`, then the guard engaging, then the log ends two lines later. So **depth submit was never the cause**. Classification is unambiguous this time: **no** `gpucrash` log, **0** `nvlddmkm` events, **0** overlay fence timeouts, no `Application Error`, no dump — i.e. the silent-exit descriptor family (`+0x1F51F5`), *not* the `0x887a0006` GPU-hang family, which always leaves two fence timeouts and a breadcrumb log. `skipped ExecuteIndirect` was **0**, so the AutoSpawnOnTerrain gate is doing its job and suppressing the NULL burst — and the crash happens anyway. That is the outcome flagged when the gate was written: the -1 descriptor arrives **alongside** that node's indirect draws, not through them, so suppressing them was never going to stop it. Next step needs a dump (ProcDump `-t`, since WER never fires on this exit); further guessing at gate points is explicitly not warranted.
* **Bug / `xr_depth_submit` could not be kept switched on, and that invalidated the test**: The launch crash (07:09:23, sessionLength 0.215 s) prompted a full log sweep, which found that `PersistLiveControlsUiState` **rewrites the whole profile and never wrote `xr_depth_submit`**. The key existed in the parser only, so every startup and every overlay save silently dropped it and restored the code default of **1 (ON)** — undoing the fix by hand within one launch. Confirmed end to end: set to 0 by hand 2026-08-24 19:33, the next session honoured it (`[DEPTH] depth submit disabled`), the profile was rewritten at 07:09:18 without it, and the crashed session that followed had **no** `[DEPTH]` line at all, i.e. depth submit was back on. So the earlier "two clean loads" result stands, but every session since had quietly reverted. A parsed-vs-written audit of `LiveControlsPoll.cpp` shows **9 keys parsed but never persisted**: `xr_depth_submit`, `xr_hand_per_frame`, `xr_hand_rel_filtered`, `xr_pitch_scale`, `xr_pitch_sign`, `xr_snap_turn_yaw_index`, `xr_sync_sequential`, and `xr_window_width`/`xr_window_height` (the last two legitimately launcher-owned). Fixed `xr_depth_submit`; the rest are recorded here rather than changed blind.
* **Correction / the launch crash is not the FOV work**: `xr_fov_mode` and its overlay control were **not** implicated. Healthy sessions reach `Session state -> 5` at line 200 and the first `NormalFOV:` at line 219-221; the crashed session reached state 5 at line 195 and ended at 198 — more than twenty lines before the camera-FOV hook first fires, so the changed code never executed. No `gpucrash` log, no `Application Error`, no dump: another silent exit, this one 0.215 s into the session.
* **Deployment**: Installed the depth-persist DLL (`73b6e8cbe1ba0bcc...`) and restored `xr_depth_submit=0` to the profile, which will now survive rewrites; backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-071415-pre-depth-persist` (DLL + ini).
* **Feature / `xr_fov_mode`, PSVR2 only**: Added a render-FOV sizing switch for the community's "very blurry picture" reports. Quantified on this PSVR2 first: eye frusta `L=-61.500 R=+43.446` (9.027 deg of cant), lens `HFov=104.946` — and cover sizing is `2*max(|L|,|R|)`, so the engine is asked for **123.000 deg**. At 3072 px that is 24.98 px/deg against 29.26 at the lens span, i.e. **a sixth of the linear resolution (37% of the solid angle) spent on frustum the wearer cannot see**. `xr_fov_mode=0` keeps cover sizing (default, unchanged); `1` asks for the de-canted span instead. Not free: the submit layer stays symmetric, so the wide side is short by the cant and an edge can appear — which is exactly the trade the black-border fix originally made. Implemented as a switch rather than a row in `kHeadsetFovDefaults` because that table's own rule is *measured, never guessed* and no single number serves both goals. **Gated on `IsRuntimePsvr2()`**: every other headset keeps shipped behaviour, since the Quest 3 already reports its true 94 deg span and a masked runtime (VDXR naming a Pico 4 "Oculus Quest2") must not be guessed at. `xr_force_fov` still overrides everything, and already offered this control undocumented — worth telling the thread asking for a slider that one exists.
* **Update / `xr_fov_mode` is live in the F10 overlay**: Added to `DrawFovControl` in the Controls panel as *"Sharper: render the lens span, not the panel cover"*, so it can be toggled in-headset without editing the ini or relaunching — the NormalFOV hook re-runs per camera update and the submit follows through `GetGameRenderFovDeg`, so both ends move together. The checkbox is drawn **only when `IsRuntimePsvr2()`**, matching the gate on the sizing itself; showing it on other headsets would be a control that silently does nothing. Its tooltip carries the measured numbers (frusta, cant, 123 vs 104.9, px/deg) and states the cost, and it greys out with a note when a manual `xr_force_fov` is set, since that overrides it. Persistence now flows through `LiveControlsUiState` like every other key rather than reading the live struct directly.
* **Deployment**: Installed the fov-mode DLL (`3b7f3021b5968142...`) and set `xr_fov_mode=1` in the active profile for testing; backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260825-070040-pre-fov-mode` (DLL + ini). **Untested in play.**

## 2026-08-24
* **Lead / depth submit was on, and it is a documented cause of this exact error**: Prompted by the user's point that guards from `psvr2-tweaks` may not have been carried forward, the guard-commit list surfaced `d44951a` *"Fix VDXR GPU device-hung crash: gate depth submit off by default"*. Its comment, still present in [OpenXRCapture.cpp](../src/Runtimes/OpenXRCapture.cpp), states the mechanism directly: copying the game's live scene-depth resource on our capture queue "races the game's own queue (which is simultaneously writing DepthPrepass/GBuffer and **reallocating render targets on load/spawn**). That cross-queue access **caused GPU device-hung (0x887a0006)**". `0x887a0006` is the exact `Device Removed Reason` in today's `gpucrash` logs, and render-target reallocation on load is exactly when the hangs occur. **The fix had been defeated by its own default**: the comment says "OFF by default (xr_depth_submit=0)" while the code ships `int xrDepthSubmit = 1` and writes `xr_depth_submit=1`, on **both** branches — so it is a long-standing comment/default mismatch, not an upstream regression. The active profile had no `xr_depth_submit` key at all, so the default applied and depth submit was live: 122 submits with `depth=1` against 75 with `depth=0`, `[DEPTH] snapshot created 3072x3072 srcFmt=19 snapFmt=40`, and the last `depth=1` only 64 lines before the hang. Set `xr_depth_submit=0` in the active profile (ini only, no rebuild); backup `%LOCALAPPDATA%\CyberpunkVRPort\config-backups\20260824-193321-pre-depth-submit-off`. **Untested.**
* **Finding / the guard's documented coverage hole is what the 19:12 hang fell through**: The hang was a **menu-initiated save load**, not ordinary gameplay as first written here. `menu=1` in the last four depth-gate samples, the guard's last window closed 2450 log lines earlier, breadcrumbs showing `HologramDepth_and_Distortion` *In progress* at `FinalFlushBarriers` with `Transparents`/`TXAA_Blurs` *Not started* — the same shape as the eight DEVICE_HUNGs of 08-10/11. This is precisely the hole [overlay-load-transition-guard](fixes/overlay-load-transition-guard.md) names: "the save-load signal ... fires only for respawn-type loads and only AFTER the load completes, so menu loads of a different save were never covered." A PSO-creation-burst trigger was drafted as the churn signal (`menu=1` itself having been measured and rejected at 59/103 samples) but was **parked untested** in favour of the depth-submit lead, to keep the experiment isolated.
* **Correction**: GPU faults today are genuinely elevated — 16 `nvlddmkm` events against 6 on 08-21 and 6 on 08-19, with none on 08-22 or 08-23 — so the user's report that today is worse is correct. Load-guard engagements per session are *not* elevated (3 in yesterday's pre-merge crash, 2-4 today), which clears the restored guard triggers of causing it. The likeliest driver of the rate is simply far more menu loads performed today, which is the uncovered path.
* **Correction / the driver-version correlation was wrong**: Repeatedly leaned on "driver dated 2026-07-22, first GPU watchdog 07-23". That conflated the driver's **release** date, which is what `Win32_PnPSignedDriver.DriverDate` reports, with its **install** date. `oem13.inf` and the NVIDIA installer record show 32.0.16.1088 was actually installed **2026-08-12 11:20**; the one before it went in 04-28. The watchdog events therefore **predate the current driver by three weeks and span two driver versions**, which removes driver version as the explanation rather than supporting it. No Windows updates were installed near 07-23 either.
* **Ruled out today, each by direct test**: the 0.1.4 merge (byte-identical crash on a pre-merge build); `DXCache` staleness (cleared 12.07 -> 0.75 GB, fault rate unchanged at 6/day); forced ReBar via NVIDIA Profile Inspector (disabled, crash recurred with the identical signature); `TdrDelay` 2 -> 8 s (applied and rebooted, the GPU still hung past 8 s — a hard hang, not a frame running long, though it did survive one stall at 19:00 before dying at 19:12).
* **Standing conclusion / this machine is a confound**: What remains is a hardware-level stability pattern, not a port defect. `0x139` `KERNEL_SECURITY_CHECK_FAILURE` subtype 3 (LIST_ENTRY corrupt) on 07-23, 07-24, 08-10 and 08-12; GPU engine timeouts continuously from 07-23 to now across two drivers; `vrserver.exe` heap corruption (`0xc0000374`); and `vrserver.exe` (SteamVR) crashing seven times in eight days. **Corrected 2026-08-24:** an earlier version of this entry also cited Stalker2/TheOuterWorlds2/SHf dumps in `CrashDumps` as evidence of machine-wide instability. Those were separate issues the user had already resolved, so they are withdrawn and must not be cited again. What survives that removal is narrower but still not port-shaped: hard GPU hangs across two driver versions plus heap corruption in the OpenXR runtime host. Note also that the `0x139` bugchecks **stop at 08-12**, the day the current driver was installed — so that update appears to have fixed the kernel corruption while leaving the GPU timeouts untouched. Remaining tests are hardware-side: Memtest86 on the 2x32 GB DDR5 (running 5000 MT/s on AM5), a GPU power-limit or core offset, and reducing per-eye resolution below 3072x3072 to test margin. **No further software lever in this port is expected to change it.**
* **Result / narrow gate works, and caught what the old guard could not**: First session on the narrow gate loaded a save successfully, played, and the `+0x1F51F5` descriptor--1 load crash **did not recur**. The gate fired exactly as designed: 9 draws on `AutoSpawnOnTerrain`, all at `sinceRebind=62ms`, `argOff` 0,20,40..., after the single re-bind at log line 517. The important detail is `args=000001F6D592D0A0` — **not NULL**. These were stale-but-valid pointers into the destroyed component, which the pre-existing NULL-argument guard could never have caught; `skipped ExecuteIndirect` count for the whole run was **0**. That is direct evidence the replay carries live-looking garbage, not just nulls, and explains why guarding NULL alone never stopped the crash. **n=1** — one session, ended by an unrelated cause, is not proof the load crash is fixed.
* **Finding / the remaining crash is the GPU one, and DXCache did not help**: The 18:16 exit was **not** the load crash. The log tail has no re-bind and no EI burst; it ends on two `Overlay fence wait timed out` lines, and `nvlddmkm` Event 153 fired three times at 18:16:56, 18:17:00 and 18:17:06 — the `VIDEO_ENGINE_TIMEOUT_DETECTED` family from the 10:04 crash. `DXCache` has been cleared and rebuilt healthily (12.07 GB -> 0.75 GB), and the GPU fault rate **did not improve**: 6 events on 08-24 against 6 on 08-21 and 6 on 08-19. That is evidence against the stale-shader-cache hypothesis and leaves the driver (32.0.16.1088, dated 2026-07-22, first watchdog dump 07-23) as the standing suspect.
* **Fix / load crash, narrow retry**: Replaced the failed broad skip with a gate on **AutoSpawnOnTerrain's indirect draws only** (`CyberpunkVR_VrcamRebindIndirectMs`, default 400 ms). The node still executes, so everything it allocates or registers still happens — that is the precise difference from the broad version, which skipped whole nodes and thereby created unallocated slots of its own. Withheld are only its `ExecuteIndirect` calls, second eye only, inside the post-rebind window. Target chosen from measurement, not guesswork: all 42 skips in the 13:01 capture were `AutoSpawnOnTerrain`, work RVA **0x77D214**, one command list, one signature, `argOff` stepping 20 from 0 to 820, `vrcamNode=1` throughout. RVA added to the verified `EngineRvas.hpp` table. Also refactored the shared re-bind timestamp to `g_vrcam_rebind_at_ms`, stamped **unconditionally**, because the previous form gated the timestamp itself on the broad consumer's setting — so turning that off would have silently disabled the narrow gate too. `CyberpunkVR_DebugVrcamIndirectGated` counts, and the first 8 gated draws log with `sinceRebind` ms. **Unverified in play.**
* **Deployment**: Installed the narrow-gate DLL (`9b9c252385278303...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-180915-pre-narrow-gate`.
* **Correction / my fix caused a new crash, reverted**: The VRCAM rebind blind window (`3fd2c1c4`, 400 ms) **made things worse and is now defaulted to 0**. It did what it was built to do — the 42-call NULL-argument indirect burst went to **zero** on the next run — but the process then died at the **first save load**, two log lines after the re-bind, where previous builds survived 4-15 minutes of play. The dump proves it is a **different crash**, not the old one: `Cyberpunk2077.exe+0x1375AE` reading `0x7E19C` (near-null plus offset) versus `+0x1F51F5` with `RDX=0xFFFFFFFF` reading a wild index-scaled address. Different RVA, different register signature, different failure class. The mechanism is the asymmetry the rule at the top of `NodeDispatch.cpp` already warns about: the skip covered **every** second-eye node including producers, and a node that never ran is a node that never allocated or registered what it was going to — leaving a slot the engine later dereferences. Suppressing the replay was plausibly manufacturing the condition it was meant to avoid. The code is kept behind a 0 default because the burst going to zero is a real measurement and still the best lead; **any retry must be narrow** — specific consuming nodes, or the indirect draws alone — never the whole second eye.
* **Fix / load crash**: The second eye no longer dispatches frame-graph nodes for a short window after a VRCAM component re-bind (`CyberpunkVR_VrcamRebindBlindMs`, default 400 ms, 0 disables). Root cause established from the ProcDump capture and the widened `[EI-DIAG]`: the component is destroyed and re-created, the frame graph still holds work referencing what it had, and **only the second eye replays it** — `vrcamNode=1` on all 42 skipped indirect draws, main view never. The argument buffers that come back NULL were already caught; one that comes back as descriptor **-1** is consumed inside `Cyberpunk2077.exe+0x1F51F5`, whose disassembly (`dec edx; imul r15,rdx,0B0h; cmp [r15+r14+5C0B60h],0`) proves a **one-based** index — so -1 is the unallocated sentinel and can never be valid, giving `r15=0xAFFFFFFF50` and an unmapped read. Skipping uses the dispatcher's existing node-cut exit and `CyberpunkVR_NodeCutRetVal`, and deliberately leaves feature bits set per the rule at the top of `NodeDispatch.cpp`. The eye simply goes stale for those frames and the existing staleness path submits MONO, which is already what happens across a load. **Unverified in play** — the mechanism is evidence-backed but the fix has not yet survived a load test. `CyberpunkVR_DebugVrcamRebindSkips` counts the skips.
* **Deployment**: Installed the rebind-blind DLL (`3fd2c1c4fe37c64a1b1cf66bc229628b9de016fbb0d0327c1ae0f4be5d28439b`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-131610-vrcam-rebind-blind`.
* **Root cause / load crash, from a real dump**: ProcDump `-t` finally captured the exit (WER LocalDumps never fired, twice). The fault is `EXCEPTION_ACCESS_VIOLATION` reading via `RDX=0xFFFFFFFF` at **`Cyberpunk2077.exe+0x1F51F5`**, caller `+0x7743C9`, **zero `CyberpunkVR_Stereo` frames anywhere on the stack**. That is byte-for-byte yesterday's 16:57 crash — same fault RVA, same `RDX=-1`, same caller, same `…0AB0` read address — which was recorded **before** the 0.1.4 merge. The merge is conclusively excluded. New evidence names the mechanism: the log ends mid-burst with **42 consecutive NULL-argument indirect skips**, all `vrcamNode=1`, all on frame-graph node **`AutoSpawnOnTerrain`**, `argOff` marching in steps of 20 (640,660,…,820), immediately after `re-bound vrcam component` at line 1296 and the guard engaging at 1297. Sequence is: VRCAM component destroyed and re-created -> second eye replays the node against resources that were never rebuilt -> the ones that come back NULL are caught by the guard, and one that comes back as descriptor **-1** is not, and Cyberpunk indexes its own `0xB0`-stride render table with it and dies. **Inference, not yet proven**: that the NULL args and the -1 descriptor share the unbuilt-resource root. The discriminator is to suppress second-eye node replay while the load guard is open, or to run mono across a load.
* **Fix / one-handed steering, from telemetry**: The `[WHEEL]` instrumentation disproved the stale-hub theory outright — `hubValid=1`, `hub=(-0.016,0.444,0.424)`, `span=0.377`, `bike=0`, stable throughout. The real cause is that a **brand-new one-handed grab has no neutral**. `v` is `hub -> controller`, so its absolute angle is wherever on the rim the hand grabbed; the continuity bias only engages on a topology change (3->2), and a grab starting 0->2 falls to the `else` branch with `bias = 0`. Measured fresh grabs opened at raw `+16.6, -26.7, -22.5, -10.3` deg — and at the user's `xr_wheel_steer_max_deg=40`, -26.7 is `out=-0.789`, i.e. 79% left lock before the hand moved. That is exactly "pulls left, will not straighten or go right": the offset consumes most of the usable range. Now zeroes the angle at the grab for one hand (`bias = -rawDeg`) and keeps absolute for two, since with two controllers the angle is genuinely meaningful. See [Mounted vehicle interaction pipeline](architecture/vehicle-interaction-pipeline.md).
* **Deployment**: Installed the steering-neutral DLL (`b2029b9cdbcb893c...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-130641-steer-neutral`. Dumps preserved on `F:\crashdumps` (2 x 15.5 GB) and the session log under `diagnostics\20260824-130157-load-crash-WITH-DUMP`.
* **Correction / failed fix**: The wheel-hub reset (`fd6ff9e7`) **did not fix** the one-handed steering pull. The stale-hub-across-vehicles hypothesis was derived from reading `WheelGrab.cpp` plus the mount/dismount trace, never measured, and it made a falsifiable prediction that failed. The reset is being kept — a per-vehicle hub is still correct and `WheelReset` genuinely had only one caller — but it is **not** the cause and must not be recorded as the fix. Cause is now open again, and this time it gets instrumented rather than reasoned about.
* **Instrumentation**: Added `[WHEEL]` telemetry (twice a second while a grab is live) printing `mask/raw/bias/deg/out/lever/nominal/hubValid/hub/span/bike` — every term feeding the steering output, so the next report distinguishes a bad hub from a bad bias from a short lever. Also widened `[EI-DIAG]` null-argument logging from `n==1 || n%500` to the first 64 occurrences, and added the frame-graph **node name and RVA**. The old throttle was actively harmful: in four consecutive load crashes the line appeared exactly once as the final log line, so the burst shape was never visible, while the measurement that originally produced the guard had seen eight calls with `argOff` marching 0,20,40..140.
* **Crash / fourth occurrence, now better characterised**: The 11:59:29 load crash ended with the same signature as 11:30, 10:42, and yesterday's pre-merge 16:57. With WER LocalDumps armed it **still produced no dump**, no `Application Error`, and no `nvlddmkm` event. That is now positive evidence rather than absence: the process is **not** dying on an unhandled exception, it is terminating silently. The open question the guard's own note ends on — "why the replay reaches an argument-less node during a load" — is the live lead, and it is second-eye-specific (`vrcamNode=1` on every occurrence; the main view never does it). A NULL `args` is caught by the guard; a **dangling** resource in the same replayed node would not be.
* **Deployment**: Installed the instrumented DLL (`d48d687c55459fa8...`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-120334-instrumented`.
* **Fix / vehicle steering**: A car steered with the left hand alone (weapon drawn) pulled permanently to one side and would not straighten or turn right. Root cause was **not** the weapon path: `g_wheelCenter`/`g_wheelSpan` are globals and `WheelReset()` had exactly one caller, the VRIK tracking on-to-off edge, so the frozen hub survived dismount and a motorcycle's handlebar geometry was inherited by the next car. It only surfaces with a weapon out because two-handed steering is right-minus-left and never reads the hub, while a drawn weapon forces the right hand off and drops steering onto the one-handed path, which does. The hub also only refreshes while both hands are off the wheel *and* the animated hands are >15 cm apart, and the one-handed pose collapses them — so the stale value is pinned precisely while in use. Reset now fires on the mounted/bike identity edge in `LocateCamera.cpp`, deliberately not on `driving`, which can flicker mid-drive where `IsDriver` is unavailable. The session log corroborates the trigger: bike at line 1345, dismount 1562, bike 2489, dismount 2570, car 2619. **Derived from code plus the mount/dismount trace, not from instrumented measurement** — there is no steering telemetry in the build. Falsifiable prediction: a fresh session going straight into a car should never show it. See [Mounted vehicle interaction pipeline](architecture/vehicle-interaction-pipeline.md).
* **Deployment**: Installed the wheel-hub-reset DLL (`fd6ff9e7b198c2cc83e5f22b35d029aed8339f9764e0ff7b88ed17635c501a23`); backup `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-114124-wheel-center-reset`. Also enabled WER LocalDumps for `Cyberpunk2077.exe` (`DumpFolder=F:\crashdumps`, `DumpType=2` full, `DumpCount=2`) — C: had only 28 GB free and a full dump is 12–20 GB, so it is pointed at F: deliberately. Note this only fires on an unhandled exception that reaches WER; the 10:42 and 11:30 exits produced no `Application Error` at all, so ProcDump with `-t` remains the only net that would catch a silent exit.
* **Crash / third occurrence**: The 11:30:51 load crash ended with the same terminal signature as 10:42 and as yesterday's 16:57 — `re-bound vrcam component` then `skipped ExecuteIndirect with NULL argument buffer`. Still no Cyberpunk dump (the LocalDumps entry was added after it). Separately `vrserver.exe` faulted at 11:31:04 in `ntdll.dll` with `0xc0000374` (**heap corruption**), 13 s after the game's post-mortem, and again at 10:36:06 in `libcrossipc.dll` with `0xc0000005`. That is SteamVR's second crash today and seventh in eight days, alongside 30 GPU watchdog timeouts since 07-23 and unrelated titles (Stalker2, TheOuterWorlds2) crashing on the same machine. Treat SteamVR/driver stability as a live confound in every crash analysis on this system.
* **Deployment**: Installed the restored-guard DLL (`d8e0604f3a2406b3fe27bd79c931c72cd6307e73cd56e08d25a275fd5252359d`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-103408-guard-triggers-restored`. Archives unchanged from the 0.1.4 push. Expect `Overlay load guard engaged/released` edges to appear more often than before -- that is the two restored arms working, not a fault; watch `CyberpunkVR_DebugOverlayGuardArms` climbing while `released` still appears.
* **Regression found and fixed / documented-but-not-implemented**: The `88bd4ef` port to 0.1.3 carried the [overlay load-transition guard](fixes/overlay-load-transition-guard.md) mechanism across but wired only **two of its four triggers**. `save load` and `swapchain invalidate` were present; **sight PSO substitution** (both the graphics-desc and stream-desc paths) and **VRCAM component re-bind** were silently dropped — the two arms added *after the eighth DEVICE_HUNG, on direct evidence*. The branch therefore ran for a fortnight on half a guard, with only the signal the concept explicitly calls insufficient ("it fires only for respawn-type loads and only after the load completes, so menu loads of a different save were never covered"). Two fossils proved it was an accident: `FrameGraph.cpp` still carried `#include "Overlay/ImGuiOverlay.hpp"   // OverlayArmLoadGuard` with no call in the file, and the "COUNTED, NOT LOGGED" note inside `OverlayArmLoadGuard` still asserts "One caller is the VRCAM component re-bind" — a caller that did not exist. Restored at `DeviceHooks.cpp:776`, `DeviceHooks.cpp:900`, and `FrameGraph.cpp:93`; the tree now has four call sites and that count is the regression check. Compiles clean under MSVC 2022 Release x64.
* **Correction / scope**: This gap did **not** cause the 10:04 crash and must not be recorded as its fix. In that session every churn event landed inside the save-load guard window by timing — VRCAM re-bind at log line 526 and four sight-PSO substitutions at 528–532, all inside the window opened at 516 and closed at 538 — and nothing unguarded preceded the stall at 636. The crash remains bugcheck `0x141`.
* **Maintenance**: Rewrote [overlay load-transition guard](fixes/overlay-load-transition-guard.md) to state which branch each claim applies to, after an earlier link repair mechanically renamed the trigger paths into files that exist but are the wrong ones for those triggers. The concept now carries a per-branch table: `psvr2-tweaks` uses `constexpr kOverlayPacingMode` in the pre-0.1.2 `src/vr/` layout with an `[OVERLAY-PACING]` self-identifying startup line and a 1000 ms drain bound; `upstream-0.1.3-psvr2` uses the exported live `CyberpunkVR_OverlayPacing`, emits **no** startup mode line and no `mode=` in `[PERF]`, and bounds the drain at 2000 ms. Regression check 1 was unperformable on this branch as written and now reads the exported symbol instead. Also recorded that the fence-timeout line is capped at four occurrences per session in code, so its absence late in a long log proves nothing.
* **Deployment**: Installed the merged upstream-0.1.4 DLL (`57ef0b0602483a1be56422dde70e950cd294f87f7b20a2f48314380eaab13d1c`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were all stopped. `sync_assets -Push` carried the rebuilt `cyberpunkvrport.archive` (1,425,408 → 1,515,520 B) into `archive\pc\mod`. Added the missing active-profile key `xr_two_hand_radius=0.060` without changing any existing setting — the profile went 56 → 57 lines and a diff against the backup shows that single added line. VRIK calibration is byte-identical (`17ca2b91…`, 479 B) and the plugin folder holds exactly one `.dll` with nothing quarantined. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-094203-upstream-0.1.4-merge` (DLL, shaders, `UserSettings.json`, `vrik_calibration.ini`, `vrport.ini`, and the pre-0.1.4 archive). Regression target: frame rate near security cameras and turrets. See [Orientation-provider and PhysicalRay hot-path frame collapse](fixes/provider-hot-path-frame-collapse.md).
* **Correction / not-a-gap**: `sync_assets.ps1` reports `vrport_player_body.archive` as *missing at source*, and this was first written up here as a standing gap worth rebuilding. That is wrong and the opposite of the intent. Upstream **deliberately backed per-bone player collision out** in 0.1.2 (`c875672`): mounting a vehicle threw it into the air, because queries-off hides the capsules from the movement system but leaves the simulation contacts, and a body parked inside the seat, door and dashboard is all interpenetration — the player cannot move, so the only thing the solver can push is the car. 16 `VRPortBody_*` colliders came out of the ep1 male player entity along with every weapon collider. The `sync_assets` entry was added in that same commit as scaffolding for an opt-in archive that was then never shipped, which is why it points at a file absent from every branch; `tools/make_player_body_ep1.py` was never committed either. The CET scripts still defensively look for `VRPortBody_` components ([HandCollision](../mods/cet/CyberpunkVRPort_HandCollision/init.lua), [Basketball](../mods/cet/CyberpunkVRPort_Basketball/init.lua)), so the runtime side is inert rather than broken. **Do not regenerate the archive to "restore" limb collision** without first solving the mounted-interpenetration case.
* **Upstream merge / performance**: Merged upstream tag `0.1.4` into `upstream-0.1.3-psvr2` with zero conflicts. The substance is two hot-path fixes: `Hooked_WaPhysicalRayOrigin` now runs its free checks before `WaPlayerOwnedWeapon`'s three `VirtualQuery` calls, and `ProvStub` only reads the out-quaternion for slots that carry the shot, the manual native override, or `XrDeepDiag`. Upstream measured 60–70 FPS collapsing to about 10 near security cameras and turrets, which evaluate both paths every frame from several worker threads. No guard was weakened — only ordering and reachability changed. The fork's PSVR2/Quest work sits in disjoint regions of the two shared files, and `WeaponAim.cpp`, `TwoHandGrip.cpp`, and `CommandListCensus.cpp` were untouched here, so nothing had to be reconciled by hand. See [Orientation-provider and PhysicalRay hot-path frame collapse](fixes/provider-hot-path-frame-collapse.md).
* **Verification boundary**: The merged tree compiles clean under MSVC 2022 Release x64 with no errors, and the fork's `xr_openxr_haptic_gain`, `xr_bike_head_offset_*`, and narrowed `xr_vehicle_throttle_trim` clamp all survive alongside upstream's new key. The frame-rate figures remain **upstream's measurements**; a PSVR2 or Quest session near a camera or turret is the outstanding regression check, and it matters more on this fork because the Quest vehicle-audio haptics worker shares the same frame budget.
* **Update**: `CyberpunkVR_TwoHandRadius` default dropped 0.12 → 0.06 and became the live ini key `xr_two_hand_radius`, clamped `[0.02, 0.30]`. At 12 cm the two-handed hold was offered to a hand merely passing the weapon. Recorded that `CyberpunkVR_CaptureMarkers`' initialiser change (1 → 0) hardens only the pre-gate window, because `ApplyLauncherDebugGate` already forced it to `0` with launcher DEBUG unticked — it was not the source of the in-play collapse.
* **Maintenance**: Repaired five stale source links in [overlay load-transition guard](fixes/overlay-load-transition-guard.md) that still used the pre-0.1.2 `src/vr/{core,overlay,stereo}/snake_case.cpp` layout. They now point at `src/Core/VrCore.cpp`, `src/Overlay/ImGuiOverlay.cpp`, and `src/Stereo/SyncStereo.cpp`, and the concept's `resource` was moved off the `psvr2-tweaks` branch. The link checker and the strict OKF v0.1 validator both pass.
* **Update**: Took upstream's rebuilt `mods/archive/cyberpunkvrport.archive` and its WolvenKit `source/raw` weapon appearance JSON, which carry the 0.1.4 fix placing a weapon's slots in both the `.app` and `.ent` files. `build_dist.ps1` ships the packed archive as-is, so packaging needed no change.

## 2026-08-23
* **Quest vehicle gun flashing / reproducible scope**: The 17:22 session reproduced hand+weapon mesh disappearance/reappearance in two different car classes after a gun was drawn, while body position and driving remained good. Stereo/VRCAM stayed healthy (`28798/28800` XR submissions), sampled masks retained `GeometrySkinned=MV` and `WeaponPlane=MV`, and there was no GPU fault. This rules against the removed low-spec INI, a particular car, and whole-view stale-VRCAM/pacing as immediate explanations, but does not yet prove pose ownership versus visibility/culling. Preserved logs under `%LOCALAPPDATA%\CyberpunkVRPort\diagnostics\20260823-173244-quest-vehicle-gun-flash`. The next no-build discriminator is toggling `F10 → VRIK → Start VR hand tracking` while flashing, then re-enabling it. See [Mounted vehicle interaction pipeline](architecture/vehicle-interaction-pipeline.md).
* **Packaging / fidelity correction**: Reclassified the historical `vrcam_cpu_tweaks.ini` as an optional low-spec profile rather than a stereo dependency. Its name/header concealed visible cuts to decals, shadows, vegetation, particles, and world streaming, with comments explicitly targeting handheld/integrated GPUs. New distributions place it under `OPTIONAL\low-spec` and `sync_assets.ps1 -Push` cannot activate it. The Ryzen 7 9800X3D / RTX 4090 / 64 GB Quest system should run without it; use matched cadence/route/save tests before opting in. Removing the active copy also gives the 16:57 load-crash retest a cleaner baseline without claiming the streaming overrides caused the invalid descriptor. See [Optional low-spec engine profile](operations/optional-low-spec-profile.md).
* **Test baseline / configuration**: Disabled the active low-spec engine override on the Ryzen 7 9800X3D / RTX 4090 Quest system after confirming Cyberpunk and helper processes were stopped. The original release file (`415882c0…`) is preserved at `%LOCALAPPDATA%\CyberpunkVRPort\config-backups\20260823-171438-vrcam-low-spec-disabled\vrcam_cpu_tweaks.ini`. Cyberpunk now uses platform/compiled defaults; no user graphics or VR settings were changed. Retest the same save/load and route before separately raising DLSS from Ultra Performance.
* **Crash finding / load transition**: Preserved the 16:57 Quest crash and exact minidump (`999f3708…`) under `%LOCALAPPDATA%\CyberpunkVRPort\crash-backups\Cyberpunk2077-20260823-165700-45752-13324`. A Cyberpunk job worker faulted at `Cyberpunk2077.exe+0x1F51F5` after receiving descriptor/index `-1` and using it as a `0xB0`-stride global render-format/resource-table index. REDengine reported `IsLoadingSavedSession=true` and `Loading world`; the VR log ended after a VRCAM component rebind and the upstream null-`ExecuteIndirect` guard. No thread RIP was in the plugin, the WASAPI haptics worker was a separate thread, OpenXR pacing was healthy, and no DRED/device-removal report accompanied it. This proves the immediate invalid render descriptor but not whether it originated in the game, another render mod, or second-eye load-transition work; reproduce before changing behavior.
* **Quest vehicle-position finding**: The reported bad car body position did not re-enable full-body VRIK: the Quest log confirms car RTTI classification, mounted arms-only ownership, and positive final-view heading samples (`1.000`, `0.987`, `0.923`, `0.919`). Current car/bike translation offsets are shared across OpenXR runtimes rather than headset-scoped, so PSVR2 would inherit the same values. Preserve them and distinguish translation axis from heading before adjustment. See [Mounted vehicle interaction pipeline](architecture/vehicle-interaction-pipeline.md).
* **Feature / vehicle haptics**: Extended non-PSVR2 OpenXR output to cars and bikes using an in-process Windows default-output loopback worker. A 28–320 Hz envelope provides sustained engine/road texture and a fast-minus-slow transient adds audible gear-change/impact bumps in both hands. Capture is gated by the existing driver state, expires after 100 ms, and never starts on PSVR2. Event hold times prevent continuous engine refreshes from truncating gun or melee pulses. `xr_openxr_vehicle_haptic_gain` (F10 Controls, default `1.0`) tunes or disables vehicle audio independently. This uses the same evidence source as the PSVR2 audio bridge but OpenXR amplitude/duration cannot reproduce Sense PCM detail. See [Generic OpenXR controller haptics](operations/generic-openxr-haptics.md).
* **Deployment**: Installed the Quest vehicle-audio haptics DLL (`7cd9b9a7defbe2e49ece447f2b61fe76d44eae05da59fe9152154e8256433664`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. Added `xr_openxr_vehicle_haptic_gain=1.00` to the active profile without changing existing settings. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260823-164034-quest-vehicle-haptics`.
* **Feature / backend isolation**: Added generic OpenXR per-hand vibration for Quest/Touch, Index, Vive, WMR, and simple-controller profiles. Confirmed per-round gun events use the existing weapon-kick ladder; two-hand grip adds weaker support feedback; physical melee swing and confirmed-impact requests share the same queue. Requests cross to the XR frame owner and are applied only while focused. Default live gain is `1.25` (`xr_openxr_haptic_gain`, F10 Controls) to compensate modestly for simple rumble without erasing weapon differences. PSVR2 rejects the generic queue before actuation and remains exclusively owned by PSVR2Toolkit. Quest left-thumbrest touch is explicitly retained as the D-pad modifier, with Triangle touch for PSVR2 and L3 as universal fallback. See [Generic OpenXR controller haptics](operations/generic-openxr-haptics.md).
* **Deployment**: Installed the Quest/OpenXR haptics build (`f879e956e038d3db0461a91a9c276b279b6730fb4c3c8640f98fdd55ee8f7c9a`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. Added the missing active-profile key `xr_openxr_haptic_gain=1.25` without changing other VR settings. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260823-161143-quest-openxr-haptics`.
* **Integration / safety fix**: Restored PSVR2Toolkit bridge motion haptics on the upstream 0.1.3 PSVR2 port without creating an OpenXR vibration action. The separately shipped bridge ABI retains `[157..160]`; reload inputs moved to `[164..167]`, while trigger override `[161]`, reload ownership `[162]`, and wheel ownership `[163]` remain distinct. A guarded bridge branch requires magic/version/live heartbeat `[168..170]`, so stale or incompatible mappings fail closed instead of turning wheel, button, or trigger changes into pulse floods. Swing pulses remain episode-gated; impact pulses now require `VRMeleeBladeHit` to return an actual contact. Gun/audio/vehicle feedback continues to mix in the bridge's single `HapticsEngine`. See [PSVR2 adaptive triggers and grip haptics](operations/psvr2-adaptive-triggers.md).
* **Deployment / startup-race fix**: Installed the matching game DLL (`25f562060d1b83aa3c3b4b1237681a1aae5cb50ce186612e10a6794922ecaa7d`) and matching weapon/reload Lua files atomically after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. The first guarded bridge exposed a normal first-frame zero-filled mapping interval as stderr; Windows PowerShell converted it to terminating `NativeCommandError`. Bridge commit `89bba70` now gives protocol publication a two-second grace and reports expected pause states on stdout. Deployed bridge SHA-256: `345f7d8118d5babfc617976969a521e6ac2739dfc0e2a0ac39b600361d62aa1f`. Backups: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260823-154145-haptics-slot-guard` and `20260823-155047-bridge-startup-race`.
* **Retest finding**: A subsequent revolver-reload test completed without another crash. This makes a deterministic failure in the immediate reload sequence less likely, but one clean retry cannot classify the preserved allocator corruption as a fluke: the original invalid allocator pointer may have been damaged earlier and merely surfaced during reload. Leave reload behavior unchanged unless the same weapon/action reproduces or new breadcrumbs identify its active stage.
* **Fix / correction**: Restored the actual PSVR2-tweaks scanner UI behavior that attribution commit `94d3ae0` had only described. A one-shot redscript wrapper now moves the complete `scannerDetailsGameController` root `-500` virtual pixels on X, bringing all scanner/quickhack detail children into view without restoring the removed CET HUD poller or disturbing HUDitor's standard-widget layout. See [VR HUD layout and scanner-details placement](fixes/center-overlay-scaling.md).
* **Crash finding**: Preserved the 14:35:49 revolver-reload crash report and full minidump (`dfeb4182…`) under the local CyberpunkVRPort crash backups. It was a main-thread read access violation in Cyberpunk's allocator (`Cyberpunk2077.exe+0x53f9d4`) while satisfying a 16-byte allocation; the allocator's linked-list pointer was already invalid (`0xAD55C9A0`). There was no GPU-crash attachment, DRED/device fault, stale-VRCAM event at termination, Lua exception, or plugin frame established in the active unwound stack. The user action makes revolver reload the leading reproduction trigger, but DEBUG was off and no reload-state breadcrumb identifies whether case spawning, speedloader attachment, or ordinary game reload was active, so causation is not yet established. Preserve the dump and reproduce against the same save/weapon before changing reload behavior.
* **Credit**: Restored explicit public README and release-description attribution to [nben/Cyberpunk-UI-mods-for-VR](https://github.com/nben/Cyberpunk-UI-mods-for-VR) for the quickhack/scanner controller and widget identification that led to targeting the complete `scannerDetailsGameController` panel.
* **Fix**: Prevented clean VR-build transitions from reapplying the packaged graphics profile. Removing `vrport.ini` recreated `first_launch=1`, and the next startup copied the entire shipped `UserSettings.json`, explaining the observed lower settings and DLSS Balanced. A prior `UserSettings.pre-vr-*.json` now proves setup already occurred, so native first-launch preserves the active profile and clears the recreated flag. See [direct installation and clean port replacement](operations/direct-install-lifecycle.md).
* **Decision**: Retained the two-hand-to-one-hand steering continuity after a repeat run made the gun/hand flashing much less pronounced without a code change. The prepared rollback was never deployed; preserve the working steering, grip-toggle, R3 throttle, vehicle-classification, and offset features while gathering more evidence.
* **Fix**: Replaced gun-mode left-stick throttle trimming with an R3 idle/restore toggle. Drawing a weapon still latches the current throttle; each right-stick click toggles it off/on, is consumed before the inverse-camera binding, and is withheld from physical reload. Left-stick Y remains free.
* **Finding**: The reported bike gun/hand texture flashing did not coincide with a stale VRCAM event, GPU fault, or missing stereo render category. The same run changed between 90 and 45 Hz while game cadence fell into roughly 41–58 FPS, making unstable reprojection of near-field controller-driven geometry the leading pacing explanation, not proven texture corruption. Test fixed SteamVR 45 Hz and compare Hand Aim on/off before changing render or skeleton behavior.
* **Fix**: Preserved manual-steering pickup across two-hand/one-hand transitions. Drawing a weapon no longer recomputes steering in the left-hand-only frame and immediately pulls the vehicle; the remaining hand inherits the current steering angle. Sensitivity remains adjustable with `xr_wheel_steer_max_deg` (lower is more sensitive).
* **Finding**: The latest vehicle log captured an initial bike-entry heading mismatch of `finalView=0.012` (about 89 degrees), followed by a later bike entry and car entry at `finalView=1.000`. This supports a transient mount/load heading issue rather than a fixed car skeleton rotation; retain F7 recenter as the safe workaround while collecting settled-entry diagnostics.
* **Fix**: Made PSVR2 left grip dual-role without the ear gesture: physical reload sees it immediately, while an unclaimed 180 ms hold becomes scanner anywhere. Scanner output is suppressed while mounted, where grip clicks now toggle each hand onto/off the wheel or handlebars instead of requiring a continuous squeeze. See the [controller input pipeline](architecture/controller-input-pipeline.md) and [PSVR2 profile](hardware/psvr2-steamvr.md).
* **Update**: Split car and motorcycle head offsets after the bike pose tested correctly but needed a small vertical adjustment. Runtime class detection selects independent F10 slider trios; the local test profile starts with bike Z at `+0.050 m` while package defaults remain zero.
* **Finding**: The matched vehicle run distinguished the poses: motorcycle animated-body/final-view alignment was `1.000`, while car entries were about `0.399`. One separate 11.984-second VRCAM stall occurred around a load/recenter, fell back to mono, rebound to a replacement component, and then captured continuously; it does not establish that later car flashing is a sustained stereo failure.
* **Fix**: Restored the native PSVR2 vehicle controls in the upstream 0.1.3 port: Sense Circle now passes through as XInput B / `ExitVehicle_Button`, and Sense Square remains X / horn instead of requiring held Square to synthesize B. See the [controller input pipeline](architecture/controller-input-pipeline.md).
* **Finding**: Vehicle-body diagnostics confirmed mounted-state detection (`mounted=1`, `driving=1`) and the intended arms-only VRIK path. The first animated-body/game-camera heading comparison was aligned (`dot=0.999`), so a blind 180-degree skeleton correction is not justified; the next build also compares against final render-view yaw.
* **Fix**: Removed the captured Russian locale from the fork's VR game-settings template and made the direct installer preserve VoiceOver, subtitle, on-screen, and platform locale values plus their list indices from the active `UserSettings.json` before first launch.
* **Creation**: Added the [direct installation and clean port replacement](operations/direct-install-lifecycle.md) playbook and release-packaged lifecycle scripts. The version-aware uninstaller removes the complete owned payload for either 0.1.1 PSVR2 or upstream 0.1.3, backs up user-adjustable/diagnostic VR state, and leaves shared dependencies installed. The adapted upstream 0.1.3 installer rejects stale separate-Hands/fork-HUD files and DXGI proxies, aligns HUDitor to upstream's F11 binding, and installs upstream's generated uninstall support files at the game root.

## 2026-08-11
* **Creation**: Melee motion haptics now work. Physical swing and impact produce per-hand pulses on katana and machete, closing a gap the audio-derived layer cannot cover — the weapon whoosh falls outside its 28-320 Hz band and is stereo-derived, so it buzzes both grips. The CET weapon mod already detected swings; a `SetVRHapticPulse` native publishes hand, amplitude and duration into shared slots `[157..160]`, and a new bridge watcher consumes them through `HapticsEngine::Pulse` so the pulses mix with the audio and semantic layers instead of competing for the actuators. Requires a bridge build carrying the watcher. See [PSVR2 adaptive triggers and grip haptics](operations/psvr2-adaptive-triggers.md).
* **Fix**: Added the [overlay load-transition guard](fixes/overlay-load-transition-guard.md). Eight `DXGI_ERROR_DEVICE_HUNG` faults, all stalling at the `FinalFlushBarriers` marker, were traced to the removal of the per-frame drain: a matched fresh-boot A/B ran clean for 6 minutes and 4 loads on the old drain and hung after 87 seconds and 1 load on middle-ground pacing. Restoring the drain only inside a bounded resource-churn window gives 70.3 fps median against 43.8 for the old drain, and a clean 8.8-minute run with 8 loads and zero fence timeouts.
* **Correction**: Marked [middle-ground frame pacing](fixes/overlay-middle-ground-pacing.md) unsafe on its own. Its validation section recorded no device fault, which was true of that single run and false of the configuration; one clean session did not establish safety.
* **Rejected hypotheses**: Concurrent Present workers corrupting backbuffer barriers, disproven by direct instrumentation — Cyberpunk serialises Present despite sixteen registered threads. A DLSS super-resolution/frame-generation version mismatch, disproven by reverting to the coherent stock 310.1 set and hanging anyway.
* **Fix**: Added the [crouch-preserving sprint gate](fixes/crouch-sprint-stance-gate.md). Sprint was derived from stick magnitude and asserted level-triggered, so ordinary forward movement on a Sense stick stood a crouched player up. Locomotion state now gates the assert; the crouch-sprint perk is declared through an F10 toggle rather than queried from TweakDB.
* **Correction**: Reversed the Sense weapon-category recommendation in [PSVR2 adaptive triggers and grip haptics](operations/psvr2-adaptive-triggers.md). The earlier preset profile was measured while Enhanced DualSense Support's UDP client was autostarting alongside the bridge; with only the bridge running the presets degrade haptics and the stock defaults are correct. UDP autostart is now locked off at the mod's default, its switch, and its config, because the default was `true` and any settings reset silently restored the conflict.
* **Finding**: Recorded that nothing besides the bridge may drive the Sense actuators. An OpenXR vibration action in the plugin cost most gun haptics even though every pulse was accepted by the runtime. Melee motion haptics remain unimplemented; the game-side detection and slots exist and the bridge already has a per-hand `Pulse` primitive to consume them.

## 2026-08-10
* **Proof of concept**: Added [Wabbajack installation automation](operations/wabbajack-automation.md). The complete 15-archive list compiled, automatically downloaded all direct and Nexus sources from an empty cache, installed with zero verifier errors, and opened a recognized Cyberpunk MO2 profile. A sibling external overwrite preserved all four user-owned configuration probes across reinstall after an in-tree overwrite was proven unsafe. The missing indexed Cyberpunk build document and real MO2-launched VR regression remain release boundaries; broad third-party `NoMatchInclude` fallback is explicitly forbidden.
* **Documentation**: Expanded [overlay middle-ground pacing](fixes/overlay-middle-ground-pacing.md) into the detailed Mode 0/1/2 implementation, ownership, crash-evidence, diagnostics, rollback, and regression record. Added the required [HUDitor VR layout workflow](operations/huditor-vr-layout.md), including Input Loader, the F7 conflict, keyboard/mouse editing, independent persistence files, and explicit credit to `nben/Cyberpunk-UI-mods-for-VR` for quickhack/scanner controller identification.
* **Maintenance**: Audited the bundle for current wording and GitHub navigation. Replaced the stale D-pad HUD-panning description with the current F10 layout and one-shot redscript design in [VR HUD layout](fixes/center-overlay-scaling.md), simplified the Sense adaptive-trigger explanation, added the [OKF maintenance and link policy](operations/okf-maintenance.md), and added `scripts/check_okf_links.py` to repair and validate document-relative links.
* **Fix / rejected experiments**: Validated [overlay middle-ground pacing](fixes/overlay-middle-ground-pacing.md): injected DXGI latency 1 held gameplay near 38–45 FPS and injected latency 2 caused `DXGI_ERROR_DEVICE_HUNG`, while the original poster's actual Mode 2—per-resource fences plus a previous-overlay fence and no injected DXGI wait—averaged about 69 FPS, reached a stable ~82 FPS segment and 96% reported GPU utilization, retained zero allocator-slot waits, and completed without a fence/device fault.
* **Finding**: Added the current [ray-tracing stereo limitation](architecture/ray-tracing-stereo-limitations.md) after ray-traced reflections appeared only in the right/VRCAM eye and ray-traced lighting made the left/MAIN eye darker. Matching render masks rule out a generic lighting gate; per-eye RT histories and outputs remain the unresolved boundary.

## 2026-08-09
* **Update**: Expanded the initial three tested weapon overrides into a conservative all-category Sense baseline and made explicit that users should choose categories rather than leave DualSense-oriented defaults, then tune every choice to taste.
* **Finding**: Native Settings category overrides produced better Sense mechanics than complex DualSense defaults; recorded Handguns = Very Soft, Shotgun = Hard, and Submachine Gun = Choppy as the initial PSVR2 compatibility profile despite the mod's DualSense-oriented Not Recommended label.
* **Update**: Added full-game stereo audio-derived Sense PCM for explosions, impacts, vehicles, ambience, and other audible gameplay, plus explicit same-mode shotgun recoil detection and stronger two-hand heavy-weapon impulses.
* **Fix**: Replaced incompatible DualSense raw Bow packing with Sense-tuned gradual weapon curves and shot-event resistance release after a `4/4` handgun profile was observed as stiff and chunky.
* **Fix**: Converted bundle-root absolute concept links to file-relative links so OKF navigation works both for bundle consumers and directly in GitHub's `.okf/` tree.
* **Creation**: Documented the optional [PSVR2 adaptive-trigger and grip-haptic integration](operations/psvr2-adaptive-triggers.md), including its extended dependencies, direct-config ownership rules, profile fidelity, launcher-DLL incompatibility, and driver recovery procedure.
* **Superseded experiment**: Extended shifted-D-pad panning to the separate `RightCenter` quickhack description panel and carried `dpadShiftActive` into the XInput hook to zero final merged right-stick axes during selection. The stick-suppression behavior remains; HUD panning was later removed as ineffective.
* **Superseded experiment**: Added shifted-D-pad horizontal HUD panning in 160-pixel steps while preserving normal D-pad input. This was later replaced by explicit F10 X/Y/Size controls and one-shot controller-specific wrappers.
* **Fix**: Added a live `Center overlays` HUD region after runtime logs showed scanner, quickhack, and popup layers arriving as unclassified centered `HUDMiddleWidget` roots; deployed a test DLL and CET script while preserving VRIK calibration and the user's existing HUD layout.

## 2026-08-08
* **Release preparation**: Created the `satyaloka93/cyberpunk-vr-port` GitHub fork, added bundled `PSVR2-CONTROLS.txt`, PS VR2 release notes, and package-script support for shipping both documents in `0.1.1-psvr2.1`.
* **Update**: Implemented and deployed UEVR-style dual-role SystemButton timing in the XInput poll: quick release emits Start, a ≥500 ms hold emits Back once, and Triangle-touch + R3 feeds the same fallback state machine while bare R3 remains crouch. Deployed DLL SHA-256: `72bb2a6459010bbff66473979403dd2a3e4b0a3d22c09ad0a7c0a232c82cde7b`; the prior DLL was retained locally; VRIK calibration remained unchanged.
* **Creation**: Added the [runtime dependency compatibility gate](operations/runtime-dependency-compatibility.md) after CET rejected Cyberpunk 2.31, leaving VRCAM disabled and output flat.
* **Update**: Updated official CET to 1.37.1, moved the VRCAM reload hotkey out of CET's `onInit`, and added one-time merged-XInput diagnostics.
* **Update**: Confirmed SteamVR's Oculus-to-Sense remapper omits Menu/System despite generated binding JSON; added Triangle-touch + R3 as a direct Start/Pause fallback while preserving bare R3 crouch.
* **Finding**: SteamVR recorded repeated Sense `Lost`/`Boot`, nine-second stuck frames, and up to 1813 dropped controller-tracker frames, confirming a separate controller tracking/Bluetooth stability problem.
* **Finding**: A manual SteamVR binding of left Create to the app's global SystemButton bypassed the broken auto-remap and opened Cyberpunk Start/Pause; SteamVR's actual system dashboard remains reserved.
* **Finding**: PSVR2 grip-pose calibration required about +26° pitch versus generic wrist defaults to level both avatar hands; persisted measured values are R `(27.4,-90,0)` and L `(-154.3,-90,0)`.
* **Finding**: Full UEVR source audit confirmed there is one OpenXR `SystemButton`, not separate Start/Menu actions; UEVR binds both menu/system candidate paths to it, maps short release to XInput Start and >500 ms hold to XInput Back, while L3+R3 opens UEVR's own framework.
* **Creation**: Added [PSVR2 Triangle-touch D-pad shifting](fixes/psvr2-triangle-dpad.md), including SteamVR binding-editor boundaries and regression checks.
* **Update**: Recorded the first PSVR2 retest and the missing early `XInputGetCapabilities` hook in the [controller input pipeline](architecture/controller-input-pipeline.md).
* **Creation**: Added the [PSVR2 SteamVR/OpenXR compatibility profile](hardware/psvr2-steamvr.md) from measured runtime and installed-mod evidence.
* **Creation**: Documented the [VRCAM resolution catalogue invariant](architecture/vrcam-resolution-catalog.md).
* **Creation**: Documented the [OpenXR controller to XInput pipeline](architecture/controller-input-pipeline.md) and packet-number fix.
* **Creation**: Scaffolded the CyberpunkVR Port Knowledge bundle with `okf_init.py`.

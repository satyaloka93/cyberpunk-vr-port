# Update Log

## 2026-08-24
* **Deployment**: Installed the merged upstream-0.1.4 DLL (`57ef0b0602483a1be56422dde70e950cd294f87f7b20a2f48314380eaab13d1c`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were all stopped. `sync_assets -Push` carried the rebuilt `cyberpunkvrport.archive` (1,425,408 → 1,515,520 B) into `archive\pc\mod`. Added the missing active-profile key `xr_two_hand_radius=0.060` without changing any existing setting — the profile went 56 → 57 lines and a diff against the backup shows that single added line. VRIK calibration is byte-identical (`17ca2b91…`, 479 B) and the plugin folder holds exactly one `.dll` with nothing quarantined. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260824-094203-upstream-0.1.4-merge` (DLL, shaders, `UserSettings.json`, `vrik_calibration.ini`, `vrport.ini`, and the pre-0.1.4 archive). Regression target: frame rate near security cameras and turrets. See [Orientation-provider and PhysicalRay hot-path frame collapse](fixes/provider-hot-path-frame-collapse.md).
* **Correction / not-a-gap**: `sync_assets.ps1` reports `vrport_player_body.archive` as *missing at source*, and this was first written up here as a standing gap worth rebuilding. That is wrong and the opposite of the intent. Upstream **deliberately backed per-bone player collision out** in 0.1.2 (`c875672`): mounting a vehicle threw it into the air, because queries-off hides the capsules from the movement system but leaves the simulation contacts, and a body parked inside the seat, door and dashboard is all interpenetration — the player cannot move, so the only thing the solver can push is the car. 16 `VRPortBody_*` colliders came out of the ep1 male player entity along with every weapon collider. The `sync_assets` entry was added in that same commit as scaffolding for an opt-in archive that was then never shipped, which is why it points at a file absent from every branch; `tools/make_player_body_ep1.py` was never committed either. The CET scripts still defensively look for `VRPortBody_` components ([HandCollision](../mods/cet/CyberpunkVRPort_HandCollision/init.lua), [Basketball](../mods/cet/CyberpunkVRPort_Basketball/init.lua)), so the runtime side is inert rather than broken. **Do not regenerate the archive to "restore" limb collision** without first solving the mounted-interpenetration case.
* **Upstream merge / performance**: Merged upstream tag `0.1.4` into `upstream-0.1.3-psvr2` with zero conflicts. The substance is two hot-path fixes: `Hooked_WaPhysicalRayOrigin` now runs its free checks before `WaPlayerOwnedWeapon`'s three `VirtualQuery` calls, and `ProvStub` only reads the out-quaternion for slots that carry the shot, the manual native override, or `XrDeepDiag`. Upstream measured 60–70 FPS collapsing to about 10 near security cameras and turrets, which evaluate both paths every frame from several worker threads. No guard was weakened — only ordering and reachability changed. The fork's PSVR2/Quest work sits in disjoint regions of the two shared files, and `WeaponAim.cpp`, `TwoHandGrip.cpp`, and `CommandListCensus.cpp` were untouched here, so nothing had to be reconciled by hand. See [Orientation-provider and PhysicalRay hot-path frame collapse](fixes/provider-hot-path-frame-collapse.md).
* **Verification boundary**: The merged tree compiles clean under MSVC 2022 Release x64 with no errors, and the fork's `xr_openxr_haptic_gain`, `xr_bike_head_offset_*`, and narrowed `xr_vehicle_throttle_trim` clamp all survive alongside upstream's new key. The frame-rate figures remain **upstream's measurements**; a PSVR2 or Quest session near a camera or turret is the outstanding regression check, and it matters more on this fork because the Quest vehicle-audio haptics worker shares the same frame budget.
* **Update**: `CyberpunkVR_TwoHandRadius` default dropped 0.12 → 0.06 and became the live ini key `xr_two_hand_radius`, clamped `[0.02, 0.30]`. At 12 cm the two-handed hold was offered to a hand merely passing the weapon. Recorded that `CyberpunkVR_CaptureMarkers`' initialiser change (1 → 0) hardens only the pre-gate window, because `ApplyLauncherDebugGate` already forced it to `0` with launcher DEBUG unticked — it was not the source of the in-play collapse.
* **Maintenance**: Repaired five stale source links in [overlay load-transition guard](fixes/overlay-load-transition-guard.md) that still used the pre-0.1.2 `src/vr/{core,overlay,stereo}/snake_case.cpp` layout. They now point at `src/Core/VrCore.cpp`, `src/Overlay/ImGuiOverlay.cpp`, and `src/Stereo/SyncStereo.cpp`, and the concept's `resource` was moved off the `psvr2-tweaks` branch. The link checker and the strict OKF v0.1 validator both pass.
* **Update**: Took upstream's rebuilt `mods/archive/cyberpunkvrport.archive` and its WolvenKit `source/raw` weapon appearance JSON, which carry the 0.1.4 fix placing a weapon's slots in both the `.app` and `.ent` files. `build_dist.ps1` ships the packed archive as-is, so packaging needed no change.

## 2026-08-23
* **Quest vehicle gun flashing / reproducible scope**: The 17:22 session reproduced hand+weapon mesh disappearance/reappearance in two different car classes after a gun was drawn, while body position and driving remained good. Stereo/VRCAM stayed healthy (`28798/28800` XR submissions), sampled masks retained `GeometrySkinned=MV` and `WeaponPlane=MV`, and there was no GPU fault. This rules against the removed low-spec INI, a particular car, and whole-view stale-VRCAM/pacing as immediate explanations, but does not yet prove pose ownership versus visibility/culling. Preserved logs under `%LOCALAPPDATA%\CyberpunkVRPort\diagnostics\20260823-173244-quest-vehicle-gun-flash`. The next no-build discriminator is toggling `F10 → VRIK → Start VR hand tracking` while flashing, then re-enabling it. See [Mounted vehicle interaction pipeline](architecture/vehicle-interaction-pipeline.md).
* **Packaging / fidelity correction**: Reclassified the historical `vrcam_cpu_tweaks.ini` as an optional low-spec profile rather than a stereo dependency. Its name/header concealed visible cuts to decals, shadows, vegetation, particles, and world streaming, with comments explicitly targeting handheld/integrated GPUs. New distributions place it under `OPTIONAL\low-spec` and `sync_assets.ps1 -Push` cannot activate it. The Ryzen 7 9800X3D / RTX 4090 / 64 GB Quest system should run without it; use matched cadence/route/save tests before opting in. Removing the active copy also gives the 16:57 load-crash retest a cleaner baseline without claiming the streaming overrides caused the invalid descriptor. See [Optional low-spec engine profile](operations/optional-low-spec-profile.md).
* **Test baseline / configuration**: Disabled the active low-spec engine override on the Ryzen 7 9800X3D / RTX 4090 Quest system after confirming Cyberpunk and helper processes were stopped. The original release file (`415882c0…`) is preserved at `%LOCALAPPDATA%\CyberpunkVRPort\config-backups\20260823-171438-vrcam-low-spec-disabled\vrcam_cpu_tweaks.ini`. Cyberpunk now uses platform/compiled defaults; no user graphics or VR settings were changed. Retest the same save/load and route before separately raising DLSS from Ultra Performance.
* **Crash finding / load transition**: Preserved the 16:57 Quest crash and exact minidump (`999f3708…`) under `%LOCALAPPDATA%\CyberpunkVRPort\crash-backups\Cyberpunk2077-20260823-165700-45752-13324`. A Cyberpunk job worker faulted at `Cyberpunk2077.exe+0x1F51F5` after receiving descriptor/index `-1` and using it as a `0xB0`-stride global render-format/resource-table index. REDengine reported `IsLoadingSavedSession=true` and `Loading world`; the VR log ended after a VRCAM component rebind and the upstream null-`ExecuteIndirect` guard. No thread RIP was in the plugin, the WASAPI haptics worker was a separate thread, OpenXR pacing was healthy, and no DRED/device-removal report accompanied it. This proves the immediate invalid render descriptor but not whether it originated in the game, another render mod, or second-eye load-transition work; reproduce before changing behavior.
* **Quest vehicle-position finding**: The reported bad car body position did not re-enable full-body VRIK: the Quest log confirms car RTTI classification, mounted arms-only ownership, and positive final-view heading samples (`1.000`, `0.987`, `0.923`, `0.919`). Current car/bike translation offsets are shared across OpenXR runtimes rather than headset-scoped, so PSVR2 would inherit the same values. Preserve them and distinguish translation axis from heading before adjustment. See [Mounted vehicle interaction pipeline](architecture/vehicle-interaction-pipeline.md).
* **Feature / vehicle haptics**: Extended non-PSVR2 OpenXR output to cars and bikes using an in-process Windows default-output loopback worker. A 28–320 Hz envelope provides sustained engine/road texture and a fast-minus-slow transient adds audible gear-change/impact bumps in both hands. Capture is gated by the existing driver state, expires after 100 ms, and never starts on PSVR2. Event hold times prevent continuous engine refreshes from truncating gun or melee pulses. `xr_openxr_vehicle_haptic_gain` (F10 Controls, default `1.0`) tunes or disables vehicle audio independently. This uses the same evidence source as the PSVR2 audio bridge but OpenXR amplitude/duration cannot reproduce Sense PCM detail. See [Generic OpenXR controller haptics](operations/generic-openxr-haptics.md).
* **Deployment**: Installed the Quest vehicle-audio haptics DLL (`7cd9b9a7defbe2e49ece447f2b61fe76d44eae05da59fe9152154e8256433664`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. Added `xr_openxr_vehicle_haptic_gain=1.00` to the active profile without changing existing settings. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260823-164034-quest-vehicle-haptics`.
* **Feature / backend isolation**: Added generic OpenXR per-hand vibration for Quest/Touch, Index, Vive, WMR, and simple-controller profiles. Confirmed per-round gun events use the existing weapon-kick ladder; two-hand grip adds weaker support feedback; physical melee swing and confirmed-impact requests share the same queue. Requests cross to the XR frame owner and are applied only while focused. Default live gain is `1.25` (`xr_openxr_haptic_gain`, F10 Controls) to compensate modestly for simple rumble without erasing weapon differences. PSVR2 rejects the generic queue before actuation and remains exclusively owned by PSVR2Toolkit. Quest left-thumbrest touch is explicitly retained as the D-pad modifier, with Triangle touch for PSVR2 and L3 as universal fallback. See [Generic OpenXR controller haptics](operations/generic-openxr-haptics.md).
* **Deployment**: Installed the Quest/OpenXR haptics build (`f879e956e038d3db0461a91a9c276b279b6730fb4c3c8640f98fdd55ee8f7c9a`) after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. Added the missing active-profile key `xr_openxr_haptic_gain=1.25` without changing other VR settings. Backup: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260823-161143-quest-openxr-haptics`.
* **Integration / safety fix**: Restored PSVR2Toolkit bridge motion haptics on the upstream 0.1.3 PSVR2 port without creating an OpenXR vibration action. The separately shipped bridge ABI retains `[157..160]`; reload inputs moved to `[164..167]`, while trigger override `[161]`, reload ownership `[162]`, and wheel ownership `[163]` remain distinct. A guarded bridge branch requires magic/version/live heartbeat `[168..170]`, so stale or incompatible mappings fail closed instead of turning wheel, button, or trigger changes into pulse floods. Swing pulses remain episode-gated; impact pulses now require `VRMeleeBladeHit` to return an actual contact. Gun/audio/vehicle feedback continues to mix in the bridge's single `HapticsEngine`. See [PSVR2 adaptive triggers and grip haptics](operations/psvr2-adaptive-triggers.md).
* **Deployment / startup-race fix**: Installed the matching game DLL (`25f562060d1b83aa3c3b4b1237681a1aae5cb50ce186612e10a6794922ecaa7d`) and matching weapon/reload Lua files atomically after confirming Cyberpunk, the bridge, DSX, and UDPClient were stopped. The first guarded bridge exposed a normal first-frame zero-filled mapping interval as stderr; Windows PowerShell converted it to terminating `NativeCommandError`. Bridge commit `89bba70` now gives protocol publication a two-second grace and reports expected pause states on stdout. Deployed bridge SHA-256: `345f7d8118d5babfc617976969a521e6ac2739dfc0e2a0ac39b600361d62aa1f`. Backups: `%LOCALAPPDATA%\CyberpunkVRPort\deploy-backups\20260823-154145-haptics-slot-guard` and `20260823-155047-bridge-startup-race`.
* **Retest finding**: A subsequent revolver-reload test completed without another crash. This makes a deterministic failure in the immediate reload sequence less likely, but one clean retry cannot classify the preserved allocator corruption as a fluke: the original invalid allocator pointer may have been damaged earlier and merely surfaced during reload. Leave reload behavior unchanged unless the same weapon/action reproduces or new breadcrumbs identify its active stage.
* **Fix / correction**: Restored the actual PSVR2-tweaks scanner UI behavior that attribution commit `94d3ae0` had only described. A one-shot redscript wrapper now moves the complete `scannerDetailsGameController` root `-500` virtual pixels on X, bringing all scanner/quickhack detail children into view without restoring the removed CET HUD poller or disturbing HUDitor's standard-widget layout. See [VR HUD layout and scanner-details placement](fixes/center-overlay-scaling.md).
* **Crash finding**: Preserved the 14:35:49 revolver-reload crash report and full minidump (`dfeb4182…`) under the local CyberpunkVRPort crash backups. It was a main-thread read access violation in Cyberpunk's allocator (`Cyberpunk2077.exe+0x53f9d4`) while satisfying a 16-byte allocation; the allocator's linked-list pointer was already invalid (`0xAD55C9A0`). There was no GPU-crash attachment, DRED/device fault, stale-VRCAM event at termination, Lua exception, or plugin frame established in the active unwound stack. The user action makes revolver reload the leading reproduction trigger, but DEBUG was off and no reload-state breadcrumb identifies whether case spawning, speedloader attachment, or ordinary game reload was active, so causation is not yet established. Preserve the dump and reproduce against the same save/weapon before changing reload behavior.
* **Credit**: Restored explicit public README and release-description attribution to [nben/Cyberpunk-UI-mods-for-VR](https://github.com/nben/Cyberpunk-UI-mods-for-VR) for the quickhack/scanner controller and widget identification that led to targeting the complete `scannerDetailsGameController` panel.
* **Fix**: Prevented clean VR-build transitions from reapplying the packaged graphics profile. Removing `vrport.ini` recreated `first_launch=1`, and the next startup copied the entire shipped `UserSettings.json`, explaining the observed lower settings and DLSS Balanced. A prior `UserSettings.pre-vr-*.json` now proves setup already occurred, so native first-launch preserves the active profile and clears the recreated flag. See [direct installation and clean port replacement](operations/direct-install-lifecycle.md).
* **Decision**: Retained the two-hand-to-one-hand steering continuity after a repeat run made the gun/hand flashing much less pronounced without a code change. The prepared rollback was never deployed; preserve the working steering, grip-toggle, R3 throttle, vehicle-classification, and offset features while gathering more evidence.
* **Fix**: Replaced gun-mode left-stick throttle trimming with an R3 idle/restore toggle. Drawing a weapon still latches the current throttle; each right-stick click toggles it off/on, is consumed before the inverse-camera binding, and is withheld from physical reload. Left-stick Y remains free.
* **Finding**: The reported bike gun/hand texture flashing did not coincide with a stale VRCAM event, GPU fault, or missing stereo render category. The same run changed between 90 and 45 Hz while game cadence fell into roughly 41–58 FPS, making unstable reprojection of near-field controller-driven geometry the leading pacing explanation, not proven texture corruption. Test fixed SteamVR 45 Hz and compare Hand Aim on/off before changing render or skeleton behavior.
* **Fix**: Preserved manual-steering pickup across two-hand/one-hand transitions. Drawing a weapon no longer recomputes steering in the left-hand-only frame and immediately pulls the vehicle; the remaining hand inherits the current steering angle. Sensitivity remains adjustable with `xr_wheel_steer_max_deg` (lower is more sensitive).
* **Finding**: The latest vehicle log captured an initial bike-entry heading mismatch of `finalView=0.012` (about 89 degrees), followed by a later bike entry and car entry at `finalView=1.000`. This supports a transient mount/load heading issue rather than a fixed car skeleton rotation; retain F7 recenter as the safe workaround while collecting settled-entry diagnostics.
* **Fix**: Made PSVR2 left grip dual-role without the ear gesture: physical reload sees it immediately, while an unclaimed 180 ms hold becomes scanner anywhere. Scanner output is suppressed while mounted, where grip clicks now toggle each hand onto/off the wheel or handlebars instead of requiring a continuous squeeze. See the [controller input pipeline](architecture/controller-input-pipeline.md) and [PSVR2 profile](hardware/psvr2-steamvr.md).
* **Update**: Split car and motorcycle head offsets after the bike pose tested correctly but needed a small vertical adjustment. Runtime class detection selects independent F10 slider trios; the local test profile starts with bike Z at `+0.050 m` while package defaults remain zero.
* **Finding**: The matched vehicle run distinguished the poses: motorcycle animated-body/final-view alignment was `1.000`, while car entries were about `0.399`. One separate 11.984-second VRCAM stall occurred around a load/recenter, fell back to mono, rebound to a replacement component, and then captured continuously; it does not establish that later car flashing is a sustained stereo failure.
* **Fix**: Restored the native PSVR2 vehicle controls in the upstream 0.1.3 port: Sense Circle now passes through as XInput B / `ExitVehicle_Button`, and Sense Square remains X / horn instead of requiring held Square to synthesize B. See the [controller input pipeline](architecture/controller-input-pipeline.md).
* **Finding**: Vehicle-body diagnostics confirmed mounted-state detection (`mounted=1`, `driving=1`) and the intended arms-only VRIK path. The first animated-body/game-camera heading comparison was aligned (`dot=0.999`), so a blind 180-degree skeleton correction is not justified; the next build also compares against final render-view yaw.
* **Fix**: Removed the captured Russian locale from the fork's VR game-settings template and made the direct installer preserve VoiceOver, subtitle, on-screen, and platform locale values plus their list indices from the active `UserSettings.json` before first launch.
* **Creation**: Added the [direct installation and clean port replacement](operations/direct-install-lifecycle.md) playbook and release-packaged lifecycle scripts. The version-aware uninstaller removes the complete owned payload for either 0.1.1 PSVR2 or upstream 0.1.3, backs up user-adjustable/diagnostic VR state, and leaves shared dependencies installed. The adapted upstream 0.1.3 installer rejects stale separate-Hands/fork-HUD files and DXGI proxies, aligns HUDitor to upstream's F11 binding, and installs upstream's generated uninstall support files at the game root.

## 2026-08-11
* **Creation**: Melee motion haptics now work. Physical swing and impact produce per-hand pulses on katana and machete, closing a gap the audio-derived layer cannot cover — the weapon whoosh falls outside its 28-320 Hz band and is stereo-derived, so it buzzes both grips. The CET weapon mod already detected swings; a `SetVRHapticPulse` native publishes hand, amplitude and duration into shared slots `[157..160]`, and a new bridge watcher consumes them through `HapticsEngine::Pulse` so the pulses mix with the audio and semantic layers instead of competing for the actuators. Requires a bridge build carrying the watcher. See [PSVR2 adaptive triggers and grip haptics](operations/psvr2-adaptive-triggers.md).
* **Fix**: Added the [overlay load-transition guard](fixes/overlay-load-transition-guard.md). Eight `DXGI_ERROR_DEVICE_HUNG` faults, all stalling at the `FinalFlushBarriers` marker, were traced to the removal of the per-frame drain: a matched fresh-boot A/B ran clean for 6 minutes and 4 loads on the old drain and hung after 87 seconds and 1 load on middle-ground pacing. Restoring the drain only inside a bounded resource-churn window gives 70.3 fps median against 43.8 for the old drain, and a clean 8.8-minute run with 8 loads and zero fence timeouts.
* **Correction**: Marked [middle-ground frame pacing](fixes/overlay-middle-ground-pacing.md) unsafe on its own. Its validation section recorded no device fault, which was true of that single run and false of the configuration; one clean session did not establish safety.
* **Rejected hypotheses**: Concurrent Present workers corrupting backbuffer barriers, disproven by direct instrumentation — Cyberpunk serialises Present despite sixteen registered threads. A DLSS super-resolution/frame-generation version mismatch, disproven by reverting to the coherent stock 310.1 set and hanging anyway.
* **Fix**: Added the [crouch-preserving sprint gate](fixes/crouch-sprint-stance-gate.md). Sprint was derived from stick magnitude and asserted level-triggered, so ordinary forward movement on a Sense stick stood a crouched player up. Locomotion state now gates the assert; the crouch-sprint perk is declared through an F10 toggle rather than queried from TweakDB.
* **Correction**: Reversed the Sense weapon-category recommendation in [PSVR2 adaptive triggers and grip haptics](operations/psvr2-adaptive-triggers.md). The earlier preset profile was measured while Enhanced DualSense Support's UDP client was autostarting alongside the bridge; with only the bridge running the presets degrade haptics and the stock defaults are correct. UDP autostart is now locked off at the mod's default, its switch, and its config, because the default was `true` and any settings reset silently restored the conflict.
* **Finding**: Recorded that nothing besides the bridge may drive the Sense actuators. An OpenXR vibration action in the plugin cost most gun haptics even though every pulse was accepted by the runtime. Melee motion haptics remain unimplemented; the game-side detection and slots exist and the bridge already has a per-hand `Pulse` primitive to consume them.

## 2026-08-10
* **Proof of concept**: Added [Wabbajack installation automation](operations/wabbajack-automation.md). The complete 15-archive list compiled, automatically downloaded all direct and Nexus sources from an empty cache, installed with zero verifier errors, and opened a recognized Cyberpunk MO2 profile. A sibling external overwrite preserved all four user-owned configuration probes across reinstall after an in-tree overwrite was proven unsafe. The missing indexed Cyberpunk build document and real MO2-launched VR regression remain release boundaries; broad third-party `NoMatchInclude` fallback is explicitly forbidden.
* **Documentation**: Expanded [overlay middle-ground pacing](fixes/overlay-middle-ground-pacing.md) into the detailed Mode 0/1/2 implementation, ownership, crash-evidence, diagnostics, rollback, and regression record. Added the required [HUDitor VR layout workflow](operations/huditor-vr-layout.md), including Input Loader, the F7 conflict, keyboard/mouse editing, independent persistence files, and explicit credit to `nben/Cyberpunk-UI-mods-for-VR` for quickhack/scanner controller identification.
* **Maintenance**: Audited the bundle for current wording and GitHub navigation. Replaced the stale D-pad HUD-panning description with the current F10 layout and one-shot redscript design in [VR HUD layout](fixes/center-overlay-scaling.md), simplified the Sense adaptive-trigger explanation, added the [OKF maintenance and link policy](operations/okf-maintenance.md), and added `scripts/check_okf_links.py` to repair and validate document-relative links.
* **Fix / rejected experiments**: Validated [overlay middle-ground pacing](fixes/overlay-middle-ground-pacing.md): injected DXGI latency 1 held gameplay near 38–45 FPS and injected latency 2 caused `DXGI_ERROR_DEVICE_HUNG`, while the original poster's actual Mode 2—per-resource fences plus a previous-overlay fence and no injected DXGI wait—averaged about 69 FPS, reached a stable ~82 FPS segment and 96% reported GPU utilization, retained zero allocator-slot waits, and completed without a fence/device fault.
* **Finding**: Added the current [ray-tracing stereo limitation](architecture/ray-tracing-stereo-limitations.md) after ray-traced reflections appeared only in the right/VRCAM eye and ray-traced lighting made the left/MAIN eye darker. Matching render masks rule out a generic lighting gate; per-eye RT histories and outputs remain the unresolved boundary.

## 2026-08-09
* **Update**: Expanded the initial three tested weapon overrides into a conservative all-category Sense baseline and made explicit that users should choose categories rather than leave DualSense-oriented defaults, then tune every choice to taste.
* **Finding**: Native Settings category overrides produced better Sense mechanics than complex DualSense defaults; recorded Handguns = Very Soft, Shotgun = Hard, and Submachine Gun = Choppy as the initial PSVR2 compatibility profile despite the mod's DualSense-oriented Not Recommended label.
* **Update**: Added full-game stereo audio-derived Sense PCM for explosions, impacts, vehicles, ambience, and other audible gameplay, plus explicit same-mode shotgun recoil detection and stronger two-hand heavy-weapon impulses.
* **Fix**: Replaced incompatible DualSense raw Bow packing with Sense-tuned gradual weapon curves and shot-event resistance release after a `4/4` handgun profile was observed as stiff and chunky.
* **Fix**: Converted bundle-root absolute concept links to file-relative links so OKF navigation works both for bundle consumers and directly in GitHub's `.okf/` tree.
* **Creation**: Documented the optional [PSVR2 adaptive-trigger and grip-haptic integration](operations/psvr2-adaptive-triggers.md), including its extended dependencies, direct-config ownership rules, profile fidelity, launcher-DLL incompatibility, and driver recovery procedure.
* **Superseded experiment**: Extended shifted-D-pad panning to the separate `RightCenter` quickhack description panel and carried `dpadShiftActive` into the XInput hook to zero final merged right-stick axes during selection. The stick-suppression behavior remains; HUD panning was later removed as ineffective.
* **Superseded experiment**: Added shifted-D-pad horizontal HUD panning in 160-pixel steps while preserving normal D-pad input. This was later replaced by explicit F10 X/Y/Size controls and one-shot controller-specific wrappers.
* **Fix**: Added a live `Center overlays` HUD region after runtime logs showed scanner, quickhack, and popup layers arriving as unclassified centered `HUDMiddleWidget` roots; deployed a test DLL and CET script while preserving VRIK calibration and the user's existing HUD layout.

## 2026-08-08
* **Release preparation**: Created the `satyaloka93/cyberpunk-vr-port` GitHub fork, added bundled `PSVR2-CONTROLS.txt`, PS VR2 release notes, and package-script support for shipping both documents in `0.1.1-psvr2.1`.
* **Update**: Implemented and deployed UEVR-style dual-role SystemButton timing in the XInput poll: quick release emits Start, a ≥500 ms hold emits Back once, and Triangle-touch + R3 feeds the same fallback state machine while bare R3 remains crouch. Deployed DLL SHA-256: `72bb2a6459010bbff66473979403dd2a3e4b0a3d22c09ad0a7c0a232c82cde7b`; the prior DLL was retained locally; VRIK calibration remained unchanged.
* **Creation**: Added the [runtime dependency compatibility gate](operations/runtime-dependency-compatibility.md) after CET rejected Cyberpunk 2.31, leaving VRCAM disabled and output flat.
* **Update**: Updated official CET to 1.37.1, moved the VRCAM reload hotkey out of CET's `onInit`, and added one-time merged-XInput diagnostics.
* **Update**: Confirmed SteamVR's Oculus-to-Sense remapper omits Menu/System despite generated binding JSON; added Triangle-touch + R3 as a direct Start/Pause fallback while preserving bare R3 crouch.
* **Finding**: SteamVR recorded repeated Sense `Lost`/`Boot`, nine-second stuck frames, and up to 1813 dropped controller-tracker frames, confirming a separate controller tracking/Bluetooth stability problem.
* **Finding**: A manual SteamVR binding of left Create to the app's global SystemButton bypassed the broken auto-remap and opened Cyberpunk Start/Pause; SteamVR's actual system dashboard remains reserved.
* **Finding**: PSVR2 grip-pose calibration required about +26° pitch versus generic wrist defaults to level both avatar hands; persisted measured values are R `(27.4,-90,0)` and L `(-154.3,-90,0)`.
* **Finding**: Full UEVR source audit confirmed there is one OpenXR `SystemButton`, not separate Start/Menu actions; UEVR binds both menu/system candidate paths to it, maps short release to XInput Start and >500 ms hold to XInput Back, while L3+R3 opens UEVR's own framework.
* **Creation**: Added [PSVR2 Triangle-touch D-pad shifting](fixes/psvr2-triangle-dpad.md), including SteamVR binding-editor boundaries and regression checks.
* **Update**: Recorded the first PSVR2 retest and the missing early `XInputGetCapabilities` hook in the [controller input pipeline](architecture/controller-input-pipeline.md).
* **Creation**: Added the [PSVR2 SteamVR/OpenXR compatibility profile](hardware/psvr2-steamvr.md) from measured runtime and installed-mod evidence.
* **Creation**: Documented the [VRCAM resolution catalogue invariant](architecture/vrcam-resolution-catalog.md).
* **Creation**: Documented the [OpenXR controller to XInput pipeline](architecture/controller-input-pipeline.md) and packet-number fix.
* **Creation**: Scaffolded the CyberpunkVR Port Knowledge bundle with `okf_init.py`.
